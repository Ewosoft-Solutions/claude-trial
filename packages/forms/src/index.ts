/**
 * @workspace/forms — the reusable Form engine schema + pure validation, shared by
 * the backend (apps/api) and the web builder/renderer. Framework-agnostic; no
 * Nest/React imports. See docs/form-engine-plan.md.
 */
export * from './schema';
export * from './validation';
export * from './person-name';
