Current Phase: Phase 2 - Dashboard Infrastructure & Role/Tenant-Aware Navigation

Current Objective:
Wire the Phase 1 design-system foundation into real authenticated surfaces:
drive the role-aware navigation model from a real ViewerContext (auth/session)
and the Next router, and build product dashboards from the shared layout and
state components.

Do not work on future phases (academic/finance/communication modules → Phase 3;
AI integrations → Phase 4). Continue reusing packages/ui; build new shared UI in
packages/ui before consuming it in apps/web.
