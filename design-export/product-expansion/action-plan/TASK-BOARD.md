# TASK BOARD — the legacy system parity

**The coordination point.** Claim a `ready` item (deps `done`) by setting Owner + Status → `claimed` in a `board: claim <ID>` commit _before_ coding. Mechanics: [`WORKFLOW.md`](WORKFLOW.md). Contract/DoD: [`/AGENTS.md`](../../../AGENTS.md). `#N` = row in [parity matrix 07](../plan/07-capability-parity-matrix.md).

**Status:** `backlog` · `ready` · `claimed` · `in-progress` · `in-review` · `done` · `blocked`
**Effort:** `S` · `M` · `L` · `XL`

## ▶ Ready to claim right now

**Foundations ready to build:** `F8` (Aurora patterns). _(`F5` (delivery) **in-review** — [PR #48](https://github.com/Ewosoft-Solutions/claude-trial/pull/48). `F6` (academic-profile/policy-version) **in-review** (claude) — built to DoD on `feat/F6-academic-profile`.)_ _(`F7` (search) **done** — [PR #44](https://github.com/Ewosoft-Solutions/claude-trial/pull/44) merged to `main`. `F1` Person + `F2` import + `F3` jobs/outbox + `F4` documents + `F7` all **done**. **WB1 (People directory)** now needs only **F8** remaining (F1 ✓ + F7 ✓).)_
_(`P0-1` **decided** + **build-first** — see [`release-1-promise.md`](release-1-promise.md); `P0-2` resequenced to onboarding. `P0-3`: **all 12 core ADRs accepted** — the 5 owner-gated ones (ADR-03/04/05/10/11) signed off **2026-08-01**, so **`F6` is now `ready`** and **WB2/WB4/WB5 + multi-campus** are unblocked at the ADR layer (they still wait on their `F`-deps). Owner also requested **verifiable/blockchain anchoring** of results + receipts → drafted as **[ADR-13](adr/ADR-13-verifiable-anchored-records.md)** (hash-only, future/deferred). Note: **ADR-10 = owner chose to _build_ an internal auditor-grade GL + integrate**, expanding WB5's finance scope.)_

---

## Hygiene (do early — unblocks trust & the states everything reuses)

| ID  | Title                                                                                                                                                  | #    | Effort | Deps | Owner | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ---- | ------ | ---- | ----- | ------ |
| H1  | Reconcile status-doc drift: permission counts (274/297/**305**) + fix stale "mock data" line in `AI_CONTEXT.md` + `CURRENT_PHASE.md`                   | —    | S      | —    | codex | done   |
| H2  | Permission-denied + empty/error/loading/offline states in `packages/ui` `custom/states`; wire `unauthorized` route (replaces the legacy system "OOPS") | #115 | M      | —    | codex | done   |
| H3  | Remove commercial/"Pay Now"-style nags from workspace surfaces; keep billing in account settings                                                       | #116 | S      | —    | codex | done   |

## Phase 0 — scope-lock + ADRs (decisions before code; no feature code)

| ID   | Title                                                                                                                                                         | Effort | Deps | Owner  | Status                                              |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- | ------ | --------------------------------------------------- |
| P0-1 | Confirm Release-1 school profile + contractual "retire the legacy system" definition ([`release-1-promise.md`](release-1-promise.md))                         | S      | —    | owner  | done                                                |
| P0-2 | Design-partner validation + per-school redacted exports — **resequenced to onboarding** (reconcile that school before its live cutover); not a pre-build gate | M      | —    | owner  | backlog                                             |
| P0-3 | Draft + accept the 12 ADRs (below)                                                                                                                            | L      | —    | claude | done · 12/12 accepted (owner signed 03/04/05/10/11 on 2026-08-01) |
| P0-4 | Stand up parity/migration/acceptance scorecards (from [06 §5](../plan/06-roadmap-and-discussion-guide.md))                                                    | S      | —    | —      | ready                                               |

### ADRs (see [`adr/README.md`](adr/README.md); draft now, some need P0-1 owner sign-off)

| ID     | Decision                                                                           | Effort | Status                  |
| ------ | ---------------------------------------------------------------------------------- | ------ | ----------------------- |
| ADR-01 | Person / auth-identity / profile / tenant-membership separation                    | M      | **accepted**            |
| ADR-02 | Class / section / offering / course-registration model (don't parse "SS1 SCIENCE") | M      | **accepted**            |
| ADR-03 | Curriculum version + tenant overlay + cohort adoption                              | M      | **accepted** (2026-08-01) |
| ADR-04 | Result publication snapshot + amendment (immutability)                             | M      | **accepted** (2026-08-01; anchoring→ADR-13) |
| ADR-05 | Finance subledger + family credit + allocation semantics                          | M      | **accepted** (2026-08-01; not custodial; posts to GL) |
| ADR-06 | Durable job/outbox infrastructure                                                  | M      | **accepted**            |
| ADR-07 | Communication delivery-provider abstraction (DND/cost/consent)                     | S      | **accepted**            |
| ADR-08 | Document/signature asset security (SigningAuthority)                               | S      | **accepted**            |
| ADR-09 | Migration source-ID + reconciliation contract                                      | M      | **accepted**            |
| ADR-10 | General-ledger **build vs integrate**                                              | M      | **accepted** (2026-08-01; build internal GL + integrate) |
| ADR-11 | Tenant vs campus/arm boundary                                                      | S      | **accepted** (2026-08-01; Option A) |
| ADR-12 | OneRoster / LTI / Ed-Fi adoption scope                                             | S      | **accepted**            |
| ADR-13 | Verifiable anchored records (results + receipts) — blockchain                      | S      | drafted (future/deferred) |
| ADR-14 | Platform surfaces over tenants (cross-tenant id · consented sharing · targeted notifications) | M | **Proposed** (owner + DPO sign-off) |

## Phase 1 — shared foundations (every later module reuses these)

| ID  | Title                                                                                                             | #           | Effort | Deps       | Owner  | Status           |
| --- | ----------------------------------------------------------------------------------------------------------------- | ----------- | ------ | ---------- | ------ | ---------------- |
| F1  | `Person`/identity/profile/membership separation + relationship history                                            | —           | XL     | ADR-01     | claude | done             |
| F2  | Shared **import & migration** platform (ImportJob/mapping/validate/commit/reconcile)                              | #26,#113    | XL     | F3, ADR-09 | claude | done             |
| F3  | Durable **job queue + transactional outbox + idempotency**                                                        | —           | L      | —          | claude | done             |
| F4  | **Document/attachment** service (scan/classify/retain/signed URLs)                                                | #28,#110    | L      | F3         | claude | done             |
| F5  | **Communication delivery** abstraction + `DeliveryAttempt` ledger (DND/cost/failure) + preferences + `SecureLink` | #97,#98,#99 | L      | F3, ADR-07 | —      | ready            |
| F6  | **Academic-profile + policy-version** framework (effective-dated)                                                 | —           | L      | ADR-03     | claude | in-review        |
| F7  | Governed **search + saved-views + URL-state directory** pattern                                                   | #105,#27    | M      | —          | claude | done             |
| F8  | Shared Aurora patterns: **Directory / Workbench / Lifecycle / Policy / Approval** in `packages/ui`                | —           | M      | —          | —      | ready            |
| F9  | Data-**export/retention/privacy** primitives (`DataExportJob`, retention)                                         | #104        | M      | F3, F4     | —      | blocked          |

**Phase 1 exit gate:** two unrelated domains use import + documents + jobs + delivery without custom copies; retry never duplicates a financial/result/message command; tenant + campus isolation tests pass; permissions enforced through one policy path.

### Phase-1 follow-ups (from the PR #42 second-agent review — non-blocking, deferred with rationale)

| ID     | Title                                                                                                                              | Effort | Deps        | Owner | Status  |
| ------ | -------------------------------------------------------------------------------------------------------------------------------- | ------ | ----------- | ----- | ------- |
| F2-fu1 | **HTTP-layer authorization tests** — e2e through the guard stack proving permission/clearance/step-up **denial** (RBAC layer, not just RLS) for People/Documents/Imports | M      | F1/F4/F2    | —     | backlog |
| F2-fu2 | **Large-import commit on the F3 job path** — route `imports.commit` through `IMPORT_COMMIT_JOB` so multi-thousand-row waves don't hit the 15 s request-tx timeout (job substrate already exists; likely WB7) | M      | F2          | —     | backlog |
| F2-fu3 | **`reconcile` state guard** — refuse reconcile before commit (currently reconciles against an empty committed set)                | S      | F2          | —     | backlog |

## Workbench 1 — Shared foundations + People (first feature slice)

| ID    | Title                                                                                                           | #      | Effort | Deps       | Owner | Status  |
| ----- | --------------------------------------------------------------------------------------------------------------- | ------ | ------ | ---------- | ----- | ------- |
| WB1-1 | Unified **People directory** (Student/Guardian/Staff/User/Prospect views)                                       | #4     | L      | F1, F7, F8 | —     | backlog |
| WB1-2 | First-class **staff employment/profile** (retire payroll-as-directory)                                          | #5     | L      | F1         | —     | backlog |
| WB1-3 | **Invitations + activation/suspension + password reset** (secure; no generated-password)                        | #1,#13 | M      | F1, F5     | —     | backlog |
| WB1-4 | **Guardianship** authority/priority/consent extension                                                           | #29    | M      | F1         | —     | backlog |
| WB1-5 | **Role editor** + `resource.action.context` matrix + permission search + presets + **effective-access preview** | #7,#8  | L      | F1, ADR-01 | —     | backlog |
| WB1-6 | **Scope + expiry + temporary cover** + maker-checker/step-up for high-risk access changes                       | #8,#9  | L      | WB1-5      | —     | backlog |

**Workbench-1 acceptance:** a staff member who is also a guardian uses **one** identity with two profiles; a bursar posts payments for Campus A but can't export Campus B debtors; a substitute gets 5-day class access that auto-expires; an admin can explain effective access (inheritance/override/scope/expiry). _(from [06 §Acceptance](../plan/06-roadmap-and-discussion-guide.md#acceptance-scenarios-that-matter))_

## Later workbenches (outlined in [`BACKLOG.md`](BACKLOG.md); detailed just-in-time)

| ID      | Workbench                                                                           | Phase | Blocked on           |
| ------- | ----------------------------------------------------------------------------------- | ----- | -------------------- |
| WB2-\*  | Academic structure + student lifecycle                                              | 2B    | F1 done; F6 (ADR-02/03 ✓) |
| WB3-\*  | Admissions                                                                          | 2C    | F1, F4, F5, WB1      |
| WB4-\*  | Results parity (ResultCycle)                                                        | 2D    | F6 (ADR-04 ✓)        |
| WB5-\*  | Family Account + Finance (**+ internal GL, ADR-10** — expect split)                 | 2E    | ADR-05 ✓, F2 done; +GL surface (ADR-10) |
| WB6-\*  | Engagement + operational reporting                                                  | 2F    | F5, F9               |
| WB7-\*  | Migration cockpit                                                                   | 2G    | F2/F4 done, ADR-09 ✓ |
| WB8-\*  | Daily-work superiority (teacher today, class workspace)                             | 3     | WB1–4                |
| WB9-\*  | Operational depth + ecosystem (library/transport/health/HR/inventory/OneRoster/LTI) | 4     | F-platforms          |
| WB10-\* | Responsible intelligence + scale                                                    | 5     | data provenance done |
| WB11-\* | **Platform-level surfaces (cross-tenant)** — inter-school participation + targeted broadcast | 4/5 | ADR-14, F5, F9, web-push backend |

---

## Change log (board edits — newest first)

- `2026-08-02` — **F6 built to DoD → in-review.** The academic-profile + policy-version framework shipped on `feat/F6-academic-profile` (ADR-03). **DB (new `curriculum` schema, hand-written migration `20260802010000`):** `CurriculumAuthority`→`CurriculumFramework`→**`CurriculumVersion`** (effective-dated + approval-state + provenance + `is_national_immutable`)→`CurriculumStage`→`CurriculumSubject`→`CurriculumNode`(strand/topic)→`LearningOutcome`, plus `CurriculumAdoption` (cohort-effective), `TenantCurriculumOverlay`, `CurriculumMapping` (aliases). **Tenanting:** the 7 reference tables carry a **nullable** `tenant_id` (NULL = shared national content) with a **two-shape RLS**: reference tables READ own+national(NULL)+platform but WRITE own+platform (so national content is immutable to tenants at the DB layer); the 3 application tables use standard isolation. Added `curriculum` to `schema.prisma` + `rls-coverage-check.sql`; `db:rls:check` green (all 10 tables). **API `CurriculumModule`:** `CurriculumService` (authoring + `assertWritableVersion` immutability guard + **activation provenance gate** — refuses to publish a version with unreviewed AI/imported nodes, fixes C081), `CurriculumAdoptionService` (adopt + **effective-dated `resolveForCohort`**), `CurriculumOverlayService`, `CurriculumMappingService` (normalized-name alias de-dup, fixes C080). 3 guarded `@TenantScoped` controllers. **Permissions +4** (`curriculum.view`/`manage`/`activate`/`adopt`; new `CURRICULUM_PERMISSIONS` array; total on this branch 320→324 — reconciles to 329 with F5's +5 at merge). **Validation (real pg, app_runtime):** curriculum e2e **5/5** (two cohorts→different versions in one campus + prior version untouched on new activation · AI-node activation gate · national readable-but-immutable + overlay path · RLS isolation · dirty-name alias resolution); api unit **470/470** (+ `normalizeName` unit); `ci:quick` + `check:privileged-db` + `db:rls:check` + seed catalog (324) green; Prettier-clean. **Next:** review → merge → F6 `done` (unblocks WB2 subject catalog + WB4 results-reference-a-version + WB8 coverage). Optional follow-ups: seed the official NERDC framework versions as production reference data; wire lesson/offering→LearningOutcome citation (WB8); resolve overlays into the read-time tree. _(claude)_
- `2026-08-02` — **F6 claimed (claude).** Building the **academic-profile + policy-version framework** to DoD on `feat/F6-academic-profile` (ADR-03): a new `curriculum` schema — `CurriculumAuthority` (NERDC/Cambridge/tenant) → `CurriculumFramework` → **`CurriculumVersion`** (effective-dated + approval state + provenance + national-immutable flag) → `CurriculumStage` → `CurriculumSubject` → `CurriculumNode` (strand/topic) → `LearningOutcome`, plus **`CurriculumAdoption`** (cohort-effective: Primary-1/JSS-1 can run different versions in one campus), **`TenantCurriculumOverlay`** (tenant edits layered over an immutable national version, never mutating the source), and **`CurriculumMapping`** (old↔canonical subject aliases for transfer/migration de-dup). National content immutable once imported; AI/imported nodes carry provenance + a named reviewer (fixes C081); an academic owner gates version activation. Server-side enforcement + RLS on every new table + effective-dated resolution + a seeded official NERDC version + e2e on real pg. _(Note: independent of F5 — branched off `main`; F5 is in-review as PR #48. Expect a board merge reconcile.)_ _(claude)_
- `2026-08-01` — **F7 → done (merged to `main`).** [PR #44](https://github.com/Ewosoft-Solutions/claude-trial/pull/44) merged (merge commit) — the governed directory pattern (`DirectoryTable` + `useDirectoryState` in `packages/ui`, tenant-owned `SavedView` + RLS, tenant+permission-filtered students projection with masked contact, audited bulk CSV export). Independent second-agent review APPROVED (3 masking bypasses found + fixed + regression-tested); CI green (api 467 unit + directory e2e + `db:rls:check` + `check:privileged-db`). Phase-1 foundations now `done`: `F1`/`F2`/`F3`/`F4`/`F7`. **WB1 (People directory) now needs only F8** and can reuse this `DirectoryTable`/`useDirectoryState` directly. _(claude)_
- `2026-08-01` — **ADR-14 drafted (Proposed) + WB11 added.** [ADR-14](adr/ADR-14-platform-surfaces-over-tenants.md) — "platform-level surfaces over tenants" — captures three related, owner+DPO-gated sub-decisions surfaced with the owner: **(14A)** cross-tenant identity (use `User.id` for account-holders now; a pseudonymous **platform participant id** when the first cross-tenant feature ships; opt-in `Person`-link only later — `Person` stays tenant-scoped, no ADR-01/11 change); **(14B)** consented cross-tenant **data sharing** for inter-school activities (SSO/OIDC-style minimized, consented **claims release** + `DisclosureLog` + retention; guardian-driven for minors; health as attestation not narrative); **(14C)** **targeted platform notifications** (platform-scoped broadcast, audience **by permission not role-name**, delivered via F5 on F3 jobs with an idempotent `DeliveryAttempt` ledger + a new device/push-subscription registry + VAPID fan-out). New **WB11-\*** (platform-level surfaces) added to later workbenches — blocked on **ADR-14 + F5 + F9 + web-push backend**. No code; docs/decision only. _(claude)_
- `2026-08-01` — **F7 built to DoD → in-review.** Governed directory pattern shipped on `feat/F7-directory-search` (PR open). **`packages/ui`:** pure `directory-state` (URL⇄state), `useDirectoryState` hook, a generic server-driven `DirectoryTable` (selection + **bulk-action bar**, sortable `aria-sort` headers, column-visibility, integrated `custom/states`, `MaskedValue`). **DB:** new `directory` schema + tenant-owned `SavedView` (hand-written RLS migration: ENABLE+FORCE + PERMISSIVE `tenant_isolation` + `app_runtime` grants + tenant-FK cascade; added to `rls-coverage-check.sql`). **API:** `DirectoryModule` — a tenant+permission-filtered students projection (contact **masked** without `students.view.personal_info`; explicit `select` that **never** touches `healthInfo`/medical/safeguarding narrative; record-level class-ownership scope; fees aggregate), `SavedViewService` (owner-scoped + tenant-shared), and a governed **bulk CSV export** (gated `students.export`, audited, masking-aware). **Web:** Students list rewired to the pattern — server-side page/filter/sort, **saved view + shareable URL + bulk export + masked contact**. **No new permissions** (reuses `students.view` / `students.view.personal_info` / `students.export`). **Validation:** api unit 464/464 · ui 147 · web 120 · directory e2e 4/4 (real pg, `app_runtime`: masking + RLS isolation + saved-view owner-scoping + export); `ci:quick` + `check:privileged-db` + `db:rls:check` green; Prettier-clean on touched files. **Next:** review → merge → F7 `done` (WB1 then needs only F8). _(claude)_
- `2026-08-01` — **P0-3 COMPLETE — all 5 owner-gated ADRs signed off → `P0-3` done (12/12).** Owner attended ADR-03/04/05/10/11 in one pass. **ADR-11 → Option A** (one tenant per operating school; campuses = orgs within; owner / campus-manager / cross-campus reporting via scoped memberships — confirms ADR-01's tenant-scoped `Person`). **ADR-03 → Option 1** (versioned curriculum + tenant overlays + cohort-effective adoption; owner deferred to eng, confirmed curricula version over time). **ADR-04 → Option 1** (immutable publication snapshot + amendment; finance never silently blocks). **ADR-05 → Option 1 with amendments** (payer/family account + allocation, **not a custodial wallet** — gateways settle to the school's account, accountants record off-app payments, "credit" = an AR balance; **checkout/cart** billing; **posts double-entry into the GL**). **ADR-10 → build internal auditor-grade GL _and_ integrate** ("no tool → use ours; has tool → integrate") — **supersedes the drafted Option-C defer**; the app is positioned as an auditing platform recording every financial transaction (ops/fees/payroll). Eng sequencing: double-entry backbone in Release-1, statement/close/payroll surface as a fast-follow (**WB5 expected to split**). **Unblocks:** `F6` → `ready`; WB2/WB4/WB5 + multi-campus cleared at the ADR layer (still gated on `F`-deps). Owner also asked for **verifiable/blockchain anchoring** of results + receipts → drafted **[ADR-13](adr/ADR-13-verifiable-anchored-records.md)** (hash-anchoring only, never PII; future/deferred). _(claude)_
- `2026-08-01` — **F7 claimed (claude).** Building the governed search + saved-views + URL-state directory pattern (`#105`/`#27`) to DoD on `feat/F7-directory-search`: a reusable Aurora `DirectoryTable` + `useDirectoryState` hook in `packages/ui`, a tenant-owned `SavedView` table (RLS), a governed tenant+permission-filtered students projection (masked contact without `students.view.personal_info`, never indexes health/safeguarding narrative), and a governed bulk action — the Students list is the first consumer (saved view + shareable URL + bulk action). No new permissions (reuses `students.view` / `students.view.personal_info` / `students.export`). _(claude)_
- `2026-08-01` — **F1 + F4 + F2 → done (merged to `main`).** [PR #42](https://github.com/Ewosoft-Solutions/claude-trial/pull/42) (Person + Documents + Imports foundations, review fixes) and [PR #43](https://github.com/Ewosoft-Solutions/claude-trial/pull/43) (changed-files format gate + editor config) both merged; branches deleted. Phase-1 foundations now on `main`: `F1`/`F2`/`F3`/`F4` done. **Unblocks WB1 (People directory** — F1+F7+F8) and the rest of Workbench-1. Remaining Phase-1 `ready`: `F5`/`F7`/`F8`; follow-ups `F2-fu1/2/3` backlog. _(claude)_
- `2026-08-01` — **PR #42 second-agent review → fixes landed; 3 follow-ups tracked.** Independent review (maker-checker) returned CHANGES-REQUESTED with 2 blocking: (1) a **security** hole — `DOCUMENT_URL_SIGNING_SECRET` shipped a constant default with no prod guard, making download tokens forgeable (any `documents.view` user could fetch any clean doc incl. signature images); (2) a **migration-abort** — the F1 back-fill's set-based `NOT EXISTS` guard would violate `students.person_id` unique when ≥2 students share one account. Both fixed + proven (prod-required secret like ENCRYPTION_KEY; `DISTINCT ON` back-fill, verified against a crafted collision). Also fixed 3 moderates (rollback no longer deletes pre-existing Persons — `import_rows.target_created`; `approve` requires validation; signature docs forced restricted) + a size-cap nit. e2e 15/15 on fresh pg; check:privileged-db + check-types green. Non-blocking follow-ups **F2-fu1/2/3** added above. _(claude)_
- `2026-08-01` — **F1 + F4 + F2 built to DoD → in-review.** Three Phase-1 foundations shipped on `feat/phase1-foundations-f1-f2-f4` → **[PR #42](https://github.com/Ewosoft-Solutions/claude-trial/pull/42)** (open, awaiting review; GitHub Actions runs CI on the PR). **F1** (ADR-01): `person` schema — Person/ContactPoint/Address/StaffProfile/GuardianRelationship/RelationshipHistory + additive `students.person_id`, RLS-safe DO-block back-fill, PersonService + PersonMergeService (dedup with preserved history), contact masking. **F4** (ADR-08): `documents` schema — Document/DocumentVersion/DocumentType/SigningAuthority/SignatureUse on the StorageProvider port, HMAC signed short-lived downloads, scan+thumbnail on F3 jobs (quarantine-until-clean), governed signature use. **F2** (ADR-09): `imports` schema — full 11-entity set, idempotent upsert-by-source-ref commit into Person (F1), source-file storage+scan via Document (F4), explicit exception queue, count/sum/checksum reconciliation, controlled rollback, step-up on commit. Permissions +15 (305→320). **Validation (real pg16-parity throwaway):** e2e F1 4/4 · F4 6/6 · F2 4/4 · F3 7/7 (serialized suite 21/21); api unit 444/444; ui 120/120; web 115/115; `db:rls:check` green (person/documents/imports covered); seed 320/320; `pnpm ci:quick` green. e2e suite set to `--runInBand` (shared-DB global job worker). **Next:** [PR #42](https://github.com/Ewosoft-Solutions/claude-trial/pull/42) open → second-agent review (L/XL) → merge; then WB1 (People directory) unblocked by F1. _(Separately, [PR #43](https://github.com/Ewosoft-Solutions/claude-trial/pull/43) adds a changed-files Prettier gate + editor format-on-save — infra, not a board item.)_ _(claude)_
- `2026-08-01` — **F4 + F1 + F2 claimed (claude).** Building Phase-1 foundations F4 (document/attachment service, ADR-08), F1 (Person/identity/profile/membership, ADR-01), and F2 (import & migration platform, ADR-09) to DoD on a single branch `feat/phase1-foundations-f1-f2-f4`. Build order F4 → F1 → F2 (F2 reuses Person + Document). Owner steers: foundation-to-DoD (models + RLS migration + server-side services/enforcement + tests + minimal REST; workbench UI deferred to WB items), full F2 entity set. _(claude)_
- `2026-08-01` — **F3 done → F2/F4/F5 unblocked.** Durable job queue + transactional outbox + idempotency shipped (ADR-06): new `jobs` schema (`jobs.jobs`, `jobs.outbox_events`) with RLS + `app_runtime` grants + `db:rls:check` coverage; `JobService.enqueue` (atomic-with-domain, idempotent via ON CONFLICT), `OutboxService.emit`, and a `JobWorker` that claims under the audited `app.is_platform` scope and runs handlers under per-job tenant scope with handler+completion in one tx (exactly-once), retry/backoff→`dead`, stale-lock reclaim. Coexists with the legacy in-memory `QueueService`. e2e 7/7 (idempotency, exactly-once, retry→dead, tx-atomic, RLS isolation); check:privileged-db + db:rls:check + ci:quick green. _(claude)_
- `2026-08-01` — **P0-3: 7 non-owner ADRs accepted → `F1` unblocked.** Reviewed + accepted ADR-01/02/06/07/08/09/12 (`Accepted — 2026-08-01`). Flips **F1 (Person) → ready** and confirms the design for the already-ready `F3`/`F7`/`F8`; F2/F5 now blocked only on F3. Reconciled a doc drift: **ADR-04 is owner-gated** (Q13–16 result-publication policy — the index had mislabelled it "no"), so the batch is **7/12**; **5 stay owner-gated** — ADR-03 (Q11), ADR-04 (Q13–16), ADR-05 (Q20–23), ADR-10 (Q20), ADR-11 (Q6) — keeping F6 + WB2/4/5 blocked. Phase-1 foundations `F1`/`F3`/`F7`/`F8` are now buildable. _(claude)_
- `2026-08-01` — **Build-first sequencing (owner).** Legacy-system screenshots are our sample of what schools use → we build from those learnings + seed data to a fully-functional demo, sell on it, then tweak per school. **P0-2 resequenced** from a pre-build gate to **onboarding** (per-school redacted exports reconcile that school before its live cutover — the Q3 rule, applied per-school at go-live). Phase-0 exit gate + [`release-1-promise.md`](release-1-promise.md) (new _Sequencing_ section) updated. Build path is unblocked; nothing waits on partner recruitment. _(claude)_
- `2026-07-31` — **P0-1 decided → done.** Owner approved the recommended defaults for decision Q1–Q3: **NG K-12** is the first full profile (tertiary/TVET/international = separate scope); "retire the legacy system" = **capability parity on the critical jobs a partner actually uses**, published as a supported/excluded matrix, not menu parity; **operational evidence + reconciliation outrank screenshots** for high-consequence behaviour. Deliverable: [`release-1-promise.md`](release-1-promise.md). Unblocks profile-specific scope in every workbench + P0-2's export requirement. **Still open:** ADR-10 (Q19–20) + ADR-11 (Q6) remain owner-gated; the definitive per-partner matrix awaits **P0-2**. _(claude)_
- `2026-07-31` — **H3 approved → done.** Audited authenticated workspaces: no commercial subscription nag ships today. Added a regression guard that rejects subscription/trial/upgrade/renewal/premium nags and the legacy expiry-linked “Pay Now” signature while preserving school-fee and finance actions. PR [#36](https://github.com/Ewosoft-Solutions/claude-trial/pull/36). _(codex)_
- `2026-07-31` — **H2 implemented; draft PR #35 open.** Added explicit Aurora permission-denied and full-surface offline presets, state-system tests, design-system examples, and shared-state wiring for `/unauthorized`. Awaiting review/merge. _(codex)_
- `2026-07-31` — **H1 implemented; draft PR #34 open.** Current status docs now reflect the wired web/API system and active product-expansion initiative; operational permission references and verification use the enforced 305-permission catalog (9 persisted categories, 28 seed groups). Root validation aliases were restored. Awaiting review/merge. _(codex)_
- `2026-07-31` — **ADR batch 3 drafted — ALL 12 ADRs now drafted.** ADR-05 (finance subledger), ADR-12 (interop) as full ADRs; ADR-10 (GL build-vs-integrate) + ADR-11 (tenant/campus) as **owner decision-briefs**. Next: review→accept (`P0-3`); owner sign-off outstanding on ADR-03/04/05/10/11. _(claude)_
- `2026-07-31` — **ADR batch 2 drafted** (ADR-02 class/offering, ADR-03 curriculum, ADR-04 result publication, ADR-09 migration) — `Proposed`. **9 of 12 ADRs now drafted**; only ADR-05 (finance), ADR-10 (GL build-vs-integrate, owner), ADR-11 (tenant/campus, owner), ADR-12 (interop) remain. _(claude)_
- `2026-07-31` — **ADR batch 1 drafted** (ADR-01 person, ADR-06 job/outbox, ADR-07 delivery, ADR-08 document/signature) — `Proposed`, awaiting review/acceptance. Accepting them flips `F1`, `F4`, `F5` (and their dependents) `blocked → ready`. _(claude)_
- `2026-07-31` — Board created from parity matrix + roadmap. All near-term items seeded; Phase-0 ADRs opened; owner-gated items marked blocked. _(claude)_
