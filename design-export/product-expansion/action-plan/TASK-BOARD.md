# TASK BOARD — the legacy system parity

**The coordination point.** Claim a `ready` item (deps `done`) by setting Owner + Status → `claimed` in a `board: claim <ID>` commit _before_ coding. Mechanics: [`WORKFLOW.md`](WORKFLOW.md). Contract/DoD: [`/AGENTS.md`](../../../AGENTS.md). `#N` = row in [parity matrix 07](../plan/07-capability-parity-matrix.md).

**Status:** `backlog` · `ready` · `claimed` · `in-progress` · `in-review` · `done` · `blocked`
**Effort:** `S` · `M` · `L` · `XL`

## ▶ Ready to claim right now

**Where we are (2026-08-17):** **Phase-1 F1–F8 `done`; `F9` (export/retention) is the only open foundation — `ready`** (deps F3+F4). **Workbench-1 complete** (WB1-1..1-6). **Workbench-2 complete** (WB2-1..2-4 — [#68]/[#71]/[#72]). **Workbench-3 core `done`** — WB3-1 pipeline + WB3-2 offer→convert ([#73] → `5a7dd00`), extended by the **structured-intake redesign merged [#78]** (`9462f2c`: cascade over the WB2-1 structure + multi-guardian + configurable staged requirements + Cloudflare R2 documents; follow-up test fix [#79] → `62dc667`), then **WB3-3 (versioned application form + typed responses) + WB3-4 (interview/exam scheduling + auto-marked admission quiz) `done`** — the admission front-door is complete; **WB3-5 (admission fee/deposit) now `ready`** (its WB5 blocker cleared by the merged finance P1). **Finance (WB5 P1)** — server-driven lists + billing/AR + fee-item catalogue **merged ([#76]/[#77])**. **352 permissions.** **Workbench-3 complete** (WB3-1..3-5) and **Workbench-4 complete pending review** — the ResultCycle spine merged as [#83], and its three surviving parity gaps (spreadsheet import · affective/psychomotor traits · cumulative transcript) are `in-review` on `feat/wb4-import-traits-transcript` (see the 2026-08-17 log entries). Formally `ready` to claim: **`F9`** (export/retention) — the only open Phase-1 foundation. **WB5 finance** in progress (P1 landed). _(The WB1-era narrative below is historical — superseded by this line.)_

**Foundations ready to build:** `F9` (export/retention) — the **only** open Phase-1 foundation, now **`ready`** (deps F3+F4 done). _(**Phase-1 foundations F1–F8 are all `done`** (F5 [#48] · F6 [#49] · F8 [#52] all merged after maker-checker review; total permissions **332** after WB1-1's `guardians.view` + WB1-3/4's `users.provision`+`guardians.manage`). **WB1-1 (People directory) is `done`** — merged via #53/#54/#56 (directory + tabs + Person 360 detail); **WB1-3 (secure provisioning) + WB1-4 (guardianship authority/consent) are `done`** — reviewed APPROVE-WITH-NITS + all corrections applied; merged via [PR #60](https://github.com/Ewosoft-Solutions/claude-trial/pull/60) (`97ac560`), CI green on `main`. **WB1-2 (first-class staff employment) + WB1-5 (role editor + effective-access evaluator) are `done`** — reviewed APPROVE-WITH-NITS + all corrections applied; merged via [PR #63](https://github.com/Ewosoft-Solutions/claude-trial/pull/63) (`7c69db5`), CI green on `main`. **WB1-6** (scope/expiry + temporary cover + maker-checker, uses `ApprovalPanel`) is now **`ready`** — its declared dep WB1-5 is `done`; it also absorbs the deferred campus-scope enforcement and the shared-catalog RLS tightening flagged in the WB1-5 review.)_ _(`F7` (search) **done** — [PR #44](https://github.com/Ewosoft-Solutions/claude-trial/pull/44) merged to `main`.)_
_(`P0-1` **decided** + **build-first** — see [`release-1-promise.md`](release-1-promise.md); `P0-2` resequenced to onboarding. `P0-3`: **all 12 core ADRs accepted** — the 5 owner-gated ones (ADR-03/04/05/10/11) signed off **2026-08-01**, so **`F6` is now `ready`** and **WB2/WB4/WB5 + multi-campus** are unblocked at the ADR layer (they still wait on their `F`-deps). Owner also requested **verifiable/blockchain anchoring** of results + receipts → drafted as **[ADR-13](adr/ADR-13-verifiable-anchored-records.md)** (hash-only, future/deferred). Note: \*\*ADR-10 = owner chose to \_build_ an internal auditor-grade GL + integrate\*\*, expanding WB5's finance scope.)\_

---

## Hygiene (do early — unblocks trust & the states everything reuses)

| ID  | Title                                                                                                                                                                                                                            | #    | Effort | Deps | Owner | Status                               |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ------ | ---- | ----- | ------------------------------------ |
| H1  | Reconcile status-doc drift: permission counts (274/297/**305**) + fix stale "mock data" line in `AI_CONTEXT.md` + `CURRENT_PHASE.md`                                                                                             | —    | S      | —    | codex | done                                 |
| H2  | Permission-denied + empty/error/loading/offline states in `packages/ui` `custom/states`; wire `unauthorized` route (replaces the legacy system "OOPS")                                                                           | #115 | M      | —    | codex | done                                 |
| H3  | Remove commercial/"Pay Now"-style nags from workspace surfaces; keep billing in account settings                                                                                                                                 | #116 | S      | —    | codex | done                                 |
| H4  | **Input-validation hardening** — validate every input by field _type_, client + server ([`docs/input-validation-plan.md`](../../../docs/input-validation-plan.md)); ~230 unbounded server strings + ~45 unvalidated web surfaces | —    | L      | —    | —     | proposed (Phase 0 partial in PR #60) |

## Phase 0 — scope-lock + ADRs (decisions before code; no feature code)

| ID   | Title                                                                                                                                                         | Effort | Deps | Owner  | Status                                                            |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- | ------ | ----------------------------------------------------------------- |
| P0-1 | Confirm Release-1 school profile + contractual "retire the legacy system" definition ([`release-1-promise.md`](release-1-promise.md))                         | S      | —    | owner  | done                                                              |
| P0-2 | Design-partner validation + per-school redacted exports — **resequenced to onboarding** (reconcile that school before its live cutover); not a pre-build gate | M      | —    | owner  | backlog                                                           |
| P0-3 | Draft + accept the 12 ADRs (below)                                                                                                                            | L      | —    | claude | done · 12/12 accepted (owner signed 03/04/05/10/11 on 2026-08-01) |
| P0-4 | Stand up parity/migration/acceptance scorecards (from [06 §5](../plan/06-roadmap-and-discussion-guide.md))                                                    | S      | —    | —      | ready                                                             |

### ADRs (see [`adr/README.md`](adr/README.md); draft now, some need P0-1 owner sign-off)

| ID     | Decision                                                                                      | Effort | Status                                                   |
| ------ | --------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------- |
| ADR-01 | Person / auth-identity / profile / tenant-membership separation                               | M      | **accepted**                                             |
| ADR-02 | Class / section / offering / course-registration model (don't parse "SS1 SCIENCE")            | M      | **accepted**                                             |
| ADR-03 | Curriculum version + tenant overlay + cohort adoption                                         | M      | **accepted** (2026-08-01)                                |
| ADR-04 | Result publication snapshot + amendment (immutability)                                        | M      | **accepted** (2026-08-01; anchoring→ADR-13)              |
| ADR-05 | Finance subledger + family credit + allocation semantics                                      | M      | **accepted** (2026-08-01; not custodial; posts to GL)    |
| ADR-06 | Durable job/outbox infrastructure                                                             | M      | **accepted**                                             |
| ADR-07 | Communication delivery-provider abstraction (DND/cost/consent)                                | S      | **accepted**                                             |
| ADR-08 | Document/signature asset security (SigningAuthority)                                          | S      | **accepted**                                             |
| ADR-09 | Migration source-ID + reconciliation contract                                                 | M      | **accepted**                                             |
| ADR-10 | General-ledger **build vs integrate**                                                         | M      | **accepted** (2026-08-01; build internal GL + integrate) |
| ADR-11 | Tenant vs campus/arm boundary                                                                 | S      | **accepted** (2026-08-01; Option A)                      |
| ADR-12 | OneRoster / LTI / Ed-Fi adoption scope                                                        | S      | **accepted**                                             |
| ADR-13 | Verifiable anchored records (results + receipts) — blockchain                                 | S      | drafted (future/deferred)                                |
| ADR-14 | Platform surfaces over tenants (cross-tenant id · consented sharing · targeted notifications) | M      | **Proposed** (owner + DPO sign-off)                      |

## Phase 1 — shared foundations (every later module reuses these)

| ID  | Title                                                                                                                                                                                                                                           | #           | Effort | Deps       | Owner  | Status               |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------ | ---------- | ------ | -------------------- |
| F1  | `Person`/identity/profile/membership separation + relationship history                                                                                                                                                                          | —           | XL     | ADR-01     | claude | done                 |
| F2  | Shared **import & migration** platform (ImportJob/mapping/validate/commit/reconcile)                                                                                                                                                            | #26,#113    | XL     | F3, ADR-09 | claude | done                 |
| F3  | Durable **job queue + transactional outbox + idempotency**                                                                                                                                                                                      | —           | L      | —          | claude | done                 |
| F4  | **Document/attachment** service (scan/classify/retain/signed URLs)                                                                                                                                                                              | #28,#110    | L      | F3         | claude | done                 |
| F5  | **Communication delivery** abstraction + `DeliveryAttempt` ledger (DND/cost/failure) + preferences + `SecureLink`                                                                                                                               | #97,#98,#99 | L      | F3, ADR-07 | claude | done                 |
| F6  | **Academic-profile + policy-version** framework (effective-dated)                                                                                                                                                                               | —           | L      | ADR-03     | claude | done                 |
| F7  | Governed **search + saved-views + URL-state directory** pattern                                                                                                                                                                                 | #105,#27    | M      | —          | claude | done                 |
| F8  | Shared Aurora patterns: **Directory / Workbench / Lifecycle / Policy / Approval** in `packages/ui`                                                                                                                                              | —           | M      | —          | claude | done                 |
| F9  | Data-**export/retention/privacy** primitives (`DataExportJob`, retention)                                                                                                                                                                       | #104        | M      | F3, F4     | —      | ready                |
| F10 | **Configurable approval workflows** — school-set steps, targeted at role / N-of-role / **chain-of-command**; generalises the hard-coded maker-checker; attachable to any sensitive action ([design](governance-approvals-and-org-structure.md)) | —           | L      | roles, F11 | —      | proposed (after WB3) |
| F11 | **Organisation structure / organogram** — multiple charts (staff/PTA/club…), units/functions, managers, members incl. parents; feeds chain-of-command + **group-targeted comms** ([design](governance-approvals-and-org-structure.md))          | —           | L      | F1         | —      | proposed (after WB3) |

**Owner-directed, captured 2026-08-11 (build after WB3; order owner-confirmed — F11 before F10):** F10 + F11 above, plus a later **role-customisation** fix (WB1-5 custom roles must allow per-permission check/uncheck, not just replicate a template). **Constraint on the current admissions work:** build the decision (offer/accept/reject) with an **approval seam** — one guardable transition, registered as a sensitive op, draftable (`request → commit`, mirroring promotion) — so F10 can wrap it later with zero refactor. Detail: [`governance-approvals-and-org-structure.md`](governance-approvals-and-org-structure.md).

**Phase 1 exit gate:** two unrelated domains use import + documents + jobs + delivery without custom copies; retry never duplicates a financial/result/message command; tenant + campus isolation tests pass; permissions enforced through one policy path.

### Phase-1 follow-ups (from the PR #42 second-agent review — non-blocking, deferred with rationale)

| ID     | Title                                                                                                                                                                                                        | Effort | Deps     | Owner | Status  |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | -------- | ----- | ------- |
| F2-fu1 | **HTTP-layer authorization tests** — e2e through the guard stack proving permission/clearance/step-up **denial** (RBAC layer, not just RLS) for People/Documents/Imports                                     | M      | F1/F4/F2 | —     | backlog |
| F2-fu2 | **Large-import commit on the F3 job path** — route `imports.commit` through `IMPORT_COMMIT_JOB` so multi-thousand-row waves don't hit the 15 s request-tx timeout (job substrate already exists; likely WB7) | M      | F2       | —     | backlog |
| F2-fu3 | **`reconcile` state guard** — refuse reconcile before commit (currently reconciles against an empty committed set)                                                                                           | S      | F2       | —     | backlog |

## Workbench 1 — Shared foundations + People (first feature slice)

| ID    | Title                                                                                                           | #      | Effort | Deps       | Owner  | Status |
| ----- | --------------------------------------------------------------------------------------------------------------- | ------ | ------ | ---------- | ------ | ------ |
| WB1-1 | Unified **People directory** (Student/Guardian/Staff/User/Prospect views)                                       | #4     | L      | F1, F7, F8 | claude | done   |
| WB1-2 | First-class **staff employment/profile** (retire payroll-as-directory)                                          | #5     | L      | F1         | claude | done   |
| WB1-3 | **Invitations + activation/suspension + password reset** (secure; no generated-password)                        | #1,#13 | M      | F1, F5     | claude | done   |
| WB1-4 | **Guardianship** authority/priority/consent extension                                                           | #29    | M      | F1         | claude | done   |
| WB1-5 | **Role editor** + `resource.action.context` matrix + permission search + presets + **effective-access preview** | #7,#8  | L      | F1, ADR-01 | claude | done   |
| WB1-6 | **Scope + expiry + temporary cover** + maker-checker/step-up for high-risk access changes                       | #8,#9  | L      | WB1-5      | claude | done   |

**Workbench-1 acceptance:** a staff member who is also a guardian uses **one** identity with two profiles; a bursar posts payments for Campus A but can't export Campus B debtors; a substitute gets 5-day class access that auto-expires; an admin can explain effective access (inheritance/override/scope/expiry). _(from [06 §Acceptance](../plan/06-roadmap-and-discussion-guide.md#acceptance-scenarios-that-matter))_

## Workbench 2 — Academic structure + student lifecycle (detailed in [`workbench-academic.md`](workbench-academic.md))

| ID    | Title                                                                                                             | #           | Effort | Deps                              | Owner  | Status |
| ----- | ----------------------------------------------------------------------------------------------------------------- | ----------- | ------ | --------------------------------- | ------ | ------ |
| WB2-1 | Academic **structure model** (ADR-02: campus·stage·year·stream·section + offerings; **label stored, not parsed**) | #36,#37,#38 | XL     | F6, ADR-02/03, **Campus (WB1-6)** | claude | done   |
| WB2-2 | **Enrollment** + per-course registration + electives + teacher assignment (profile selects which)                 | #30         | L      | WB2-1, F6                         | claude | done   |
| WB2-3 | Student **lifecycle**: registration · transfer · withdrawal · graduation (durable, effective-dated history)       | #31,#32     | L      | WB2-2, F1                         | claude | done   |
| WB2-4 | **Promotion workbench** (year rollover: preview + exceptions, on F3 jobs + maker-checker commit)                  | #58         | L      | WB2-3                             | claude | done   |

**Workbench-2 acceptance:** SS1 ARTS vs SS1 SCIENCE model without name-parsing; a K-12 (class) and a tertiary (per-course) profile both resolve student→subject; a mid-year transfer keeps both placements; a promotion preview + exceptions commits next-year enrollments leaving the prior year untouched; an admin can explain a student's placement + year-over-year history. _(detail: [`workbench-academic.md`](workbench-academic.md))_

## Workbench 3 — Admissions (detailed in [`workbench-admissions.md`](workbench-admissions.md))

_Replaces the flat page-first `AdmissionApplication` stub (strings-only stage/decision + a grandfathered privileged-client read) with a durable, Person-linked pipeline that ends in a one-command conversion to a registered student (reusing the WB2-3 lifecycle). Parity jobs 14–23 (matrix 07 §Admissions). Deps F1/F4/F5/WB1 + WB2 all `done`._

| ID    | Title                                                                                                                                                                               | #       | Effort | Deps              | Owner  | Status |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------ | ----------------- | ------ | ------ |
| WB3-1 | Admissions **pipeline + decision history** (durable stage machine + effective-dated stage events + scored `AdmissionReview`; retires the strings-only stub + its privileged client) | #14,#17 | L      | F1, WB1           | claude | done   |
| WB3-2 | **Offer → acceptance → one-command conversion → student** (offer/accept states + `convertToStudent` = Person+profile+Student + register into a section via the WB2-3 lifecycle)     | #22,#23 | M      | WB3-1, WB2-3, F1  | claude | done   |
| WB3-3 | **Versioned application form + responses** (form builder + typed responses)                                                                                                         | #15     | L      | WB3-1             | claude | done   |
| WB3-4 | **Interview/exam scheduling + outcome** + admission **quiz** (reuse `Assessment` marker)                                                                                            | #18,#19 | M      | WB3-1             | claude | done   |
| WB3-5 | **Admission fee/deposit** (link Finance)                                                                                                                                            | #20     | M      | WB3-1, WB5 (P1 ✓) | claude | done   |

**Workbench-3 acceptance (this session's build = WB3-1 + WB3-2):** an application is a durable, Person-linked record moving through explicit stages with an **auditable stage + decision/review history** (never strings-only); a reviewer records a **scored decision** that is kept; an accepted applicant gets an **offer** then, on acceptance, **one command converts them into a registered student** placed into a class section via the WB2-3 lifecycle (the new student shows a proper registration placement span); tenant isolation + campus scope + server-side permissions hold; **no privileged client** (the stub's grandfathered read is removed). _(detail: [`workbench-admissions.md`](workbench-admissions.md))_

## Workbench 4 — Results parity / ResultCycle (ADR-04)

_The immutable result-publication layer over the day-to-day gradebook. Parity jobs 53–66 (matrix 07 §Results & grading). Deps F6 ✓ (results reference a curriculum version) + WB2 ✓ (offerings + sections + promotion input)._

| ID    | Title                                                                                                                                                                                                                            | #                                           | Effort | Deps    | Owner  | Status    |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | ------ | ------- | ------ | --------- |
| WB4-1 | **ResultCycle spine** — configure → enter → validate/moderate → maker-checker **publish (immutable snapshot)** → amend/supersede; `FinancialHold`; report-card/broadsheet artifacts; promotion recommendation; ranking-as-policy | #53,#55,#56,#57,#58,#59,#60,#61,#62,#65,#66 | XL     | F6, WB2 | claude | done      |
| WB4-2 | **Bulk score import** (CSV/XLSX) — direct entry + Excel import as ONE flow: template → dry-run report → commit through the same entry writer                                                                                     | #54                                         | M      | WB4-1   | claude | in-review |
| WB4-3 | **Affective/psychomotor traits** — a cycle-configurable behavioural rubric, rated per student, snapshotted into the publication + rendered on the report card                                                                    | #57 (partial), #64 seam                     | M      | WB4-1   | claude | in-review |
| WB4-4 | **Cumulative transcript** — a student's multi-year record built ONLY from published (non-superseded) snapshots, checksum-carrying, stored as an immutable artifact                                                               | #63,#66                                     | M      | WB4-1   | claude | in-review |

**Workbench-4 acceptance:** a term's results are configured, captured (keyed **or imported from a spreadsheet**, with absent ≠ zero preserved through the import), validated for completeness, moderated, and published by a **second approver** as an immutable, checksum-addressed snapshot with a report card per student and a section broadsheet; a correction after publication is an **amendment** that supersedes rather than overwrites; a family's result can be **reproduced and explained** months later from the snapshot alone; a student's **transcript** across years reads only from published snapshots; result visibility is gated only by an explicit, audited `FinancialHold`.

## Workbench 5 — Family Account + Finance (ADR-05 subledger + ADR-10 internal GL)

_The receivables subledger a school actually runs on — itemised bills, family-level payment, real outstanding balances — and the double-entry backbone underneath it that makes the books auditor-grade. Parity jobs 77–87, 95 (matrix 07 §Finance & accounting). Deps ADR-05 ✓ + ADR-10 ✓ (owner chose **build an internal GL and integrate**), F2 ✓, WB1-6 maker-checker ✓._

**Release-1 cut line (ADR-10 sequencing):** receivables subledger + manual/off-app receipts + **double-entry journal posting** + trial balance + reconciliation to control totals + an accounting **export** adapter. Payroll, AP/expense, budgets, period close and the full statement surface are the fast-follow workbench; a live payment-gateway integration waits on an owner decision (WB5-7).

| ID    | Title                                                                                                                                                                                     | #            | Effort | Deps          | Owner  | Status  |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ------ | ------------- | ------ | ------- |
| WB5-1 | **Itemised invoices + adjustments** (P1) — fee-item catalogue, invoice lines, discount policies + discretionary adjustments through maker-checker, **derived** balances                    | #77,#78      | L      | F2            | claude | done    |
| WB5-2 | **Billing households** (P2) — the durable family account: temporal membership + payers, auto-derived from guardian clusters, operator merge                                                | #79 (part)   | M      | WB5-1         | claude | done    |
| WB5-3 | **Receipts + allocations** (P3) — a receipt is money *received*, allocated across many invoices/siblings; family checkout; gap-aware, never-reused **receipt numbering** + reprint audit   | #80,#82,#95  | L      | WB5-2         | claude | in-review |
| WB5-4 | **Unapplied credit** (P4) — overpayment parks as an AR **credit balance** on the household (never stored value), auto-applied to the next invoice; cash refunds stay deferred              | #81          | M      | WB5-3         | claude | in-review |
| WB5-5 | **Double-entry GL backbone** (ADR-10) — chart of accounts, journal entries/lines, accounting periods; every receivables event posts a **balanced** entry; trial balance; contra reversals   | #87,#95      | L      | WB5-3         | claude | in-review |
| WB5-6 | **Collections + reconciliation** — daily collection, outstanding **aging**, subledger-vs-GL control-total reconciliation, and the accounting **export** adapter (ADR-12, never credentials) | #103 (finance) | M    | WB5-5         | claude | in-review |
| WB5-7 | **Payment gateway + idempotent webhooks** — signed adapter, settlement into the school's own account                                                                                       | #85          | M      | WB5-3         | —      | blocked |
| WB5-8 | **Opening-balance import** — brought-forward debt as invoices/credits with source refs                                                                                                     | #91 (C091)   | M      | WB7 cockpit   | —      | backlog |

**WB5-7 is blocked on the owner, not on code:** which gateway (Paystack / Flutterwave / other) the design partners use, and the webhook signing secret + merchant account for each environment. The ingestion shape is provider-specific and the secret is boot-required, so building it against a guessed provider would be throwaway work.

**Workbench-5 acceptance:** a parent pays **once** for two siblings and the receipt shows exactly which invoice each naira settled; a part-payment leaves a real outstanding balance that a later payment or an approved waiver finishes; an overpayment becomes **credit**, not income, and lands on the next invoice; an approved discount changes the outstanding balance but never the original charge, and is undone only by a reversal; **every** one of those events has a balanced journal entry behind it, the trial balance nets to zero, and the AR control account agrees with the sum of open invoices to the naira.

## Later workbenches (outlined in [`BACKLOG.md`](BACKLOG.md); detailed just-in-time)

| ID      | Workbench                                                                                       | Phase | Blocked on                              |
| ------- | ----------------------------------------------------------------------------------------------- | ----- | --------------------------------------- |
| WB2-\*  | Academic structure + student lifecycle — **detailed above; WB2-1..WB2-4 all `done` (complete)** | 2B    | ✓ done (#68/#71/#72)                    |
| WB3-\*  | Admissions                                                                                      | 2C    | F1, F4, F5, WB1                         |
| WB4-\*  | Results parity (ResultCycle) — **detailed above; WB4-1 `done` (#83)**                           | 2D    | ✓ unblocked (F6 ✓, WB2 ✓)               |
| WB5-\*  | Family Account + Finance (**+ internal GL, ADR-10**) — **detailed above; WB5-1..5-6 built — 5-3..5-6 `in-review` as [#129], 5-7 blocked on the owner** | 2E    | ✓ unblocked (ADR-05/ADR-10, F2)         |
| WB6-\*  | Engagement + operational reporting                                                              | 2F    | F5, F9                                  |
| WB7-\*  | Migration cockpit                                                                               | 2G    | F2/F4 done, ADR-09 ✓                    |
| WB8-\*  | Daily-work superiority (teacher today, class workspace)                                         | 3     | WB1–4                                   |
| WB9-\*  | Operational depth + ecosystem (library/transport/health/HR/inventory/OneRoster/LTI)             | 4     | F-platforms                             |
| WB10-\* | Responsible intelligence + scale                                                                | 5     | data provenance done                    |
| WB11-\* | **Platform-level surfaces (cross-tenant)** — inter-school participation + targeted broadcast    | 4/5   | ADR-14, F5, F9, web-push backend        |

---

## Change log (board edits — newest first)

- `2026-08-19` — **WB5-3..5-6 pushed → [PR #129](https://github.com/Ewosoft-Solutions/claude-trial/pull/129) opened against `main`.** 19 commits, 86 files. Carries the four slices plus a cross-cutting **error-hygiene fix** found while driving the branch by hand: production now suppresses class-validator's generated messages (an `@IsIn` failure was answering with the platform's whole sensitive-operation catalogue, which reached a user in a dialog), and one shared client guard renders only a plain, human-sized string — so a validation array or a load balancer's HTML 502 can never be shown to an operator, while deliberate domain messages survive. **Operational notes on the PR:** five migrations in order (the `…150000` prune must precede the `…160000` constraints), permissions **352 → 354** and sensitive ops **32 → 34** (seed per environment), and **the API must be restarted after the merge** — the sensitive-operation catalogue is compiled in from `packages/database`, so a stale process rejects the new step-up operations. _(claude)_

- `2026-08-19` — **WB5 third review round — a second, UNANCHORED reviewer → 9 majors, all fixed.** A fresh agent (no knowledge of the earlier findings) was pointed at the seams review usually misses: the web layer, the money-input boundary, the seeds, the migration path. Only one finding overlapped anything seen before. Fixed: (a) **`applyCredit` never checked whose credit it was** — one family's overpayment could settle another's bill, and every control total still read zero because the entry balances either way; (b) approving an adjustment **opened the books after applying it**, so a school whose first act was a waiver opened short by the discount, permanently; (c) a **reversal could itself be reversed**, putting a cancelled invoice's receivable back; (d) an entry made on a period's **final day fell outside the period**, and so slipped past the closed-period check; (e) **merging households orphaned** the source's credits and receipts where nothing could find them again, while reconciliation kept reporting the money as held; (f) recording money had **no idempotency** — a lost response on a prepayment let a second click take it twice; (g) issuing/cancelling an invoice wrote **no audit row**, and the **opening entry was posted by nobody**, triggered by a `GET`; (h) **"Define period" could never succeed** — pt. 2 added step-up to the route and left the dialog unable to answer it, making the whole period-close feature unreachable; (i) a **prune migration now runs before the CHECK constraints**, so a legacy zero-amount row cannot wedge `migrate deploy` mid-upgrade. Plus ~20 minors: export gated + filter-aware, Reverse hidden where the server refuses it, searchable family picker, unticking a child re-syncs the amount, money input refuses what it cannot parse, a failed balance fetch renders an error not a confident zero, seeds write real line items (every seeded invoice had gross 0). **Tests got honest:** a real two-transaction concurrency proof, a real opening-balance race, and a cross-tenant write — the previous versions passed with the locks and the unique index removed. **Verify:** api unit **861** · finance e2e **18/18** · authorization **5/5** · `ci:quick` ✔ · all gates ✔. **Next:** merge decision. _(claude)_

- `2026-08-19` — **WB5 confirmation pass → 3 new drifts + a migration-rule violation, all fixed.** The reviewer re-read the fixes and confirmed the seven majors were genuinely addressed — then found what the fixes had opened, which is the half that matters: (a) cancelling a bill that **predates the ledger** reversed its waiver and then withdrew the already-discounted balance, stranding the discount in receivables (the two cases are now two branches, and a new e2e covers the pre-ledger one); (b) **two standing discount policies** were each capped against the same untouched balance, so a 60% scholarship plus a 50% sibling discount credited 110% of the charge (the cap now accumulates); (c) the two credit paths locked **invoice/credit in opposite orders** — an ABBA deadlock on a money route — now one global order, and **adjustment approval takes the lock it was missing**, which was also the last reachable path to a stranded overpayment. Plus the rule I broke myself: correcting an **already-applied migration** in place (AGENTS.md §7). Reverted; the correction is now `20260819170000_wb5_backfill_correction`, which also repairs environments that ran the original. The P2002 catches now name their constraint and raise a retryable conflict instead of pretending to recover inside an aborted transaction. **The unauthorized-scope DoD gap is properly closed** — new `finance-authorization-http.e2e-spec.ts` proves real **403s** through the guard stack (ledger routes refused to a `finance.view` holder; recording money refused for want of step-up), not just decorator metadata. Two findings were argued rather than fixed and the reviewer accepted both. **Verify:** api unit **859** · **e2e 34 suites / 246 tests, 0 failures** (finance 17/17, authorization 5/5) · `ci:quick` ✔ · `db:rls:check` ✔ · `check:privileged-db` ✔ (27) · `db:verify` ✔. **Next:** merge decision; WB5-7 (gateway provider + signing secret) remains the only owner-gated item. _(claude)_

- `2026-08-19` — **WB5 independent review → CHANGES-REQUESTED (7 major) → all fixed, re-verified.** A reviewer with no stake in the branch traced the paths the happy-path e2e never walks, and every major finding ended the same way: the AR control account permanently disagreeing with the invoices. Fixed: (1) billing an **admission fee** opened the books *after* writing the invoice, counting that bill twice — the same ordering defect the previous entry claims to have fixed, missed in the one place it also applied; (2) **re-issuing** an already part-paid invoice re-posted the charge, re-applied every standing policy discount and re-drew the family's credit — issuing is now a **draft-only** transition; (3) an approved **waiver was bounded by nothing**, so waiving past what was owed pushed receivables below zero and left the family owed money held in no account — now bounded at request *and* re-checked at approval, because the balance moves while an approval waits; (4) **cancelling** reversed only the charge (leaving any waiver posted against it standing) and did nothing at all for an invoice billed before the ledger opened — the common case for a school that upgrades; (5) **two cashiers** settling the same invoice in the same second both saw the full outstanding and both wrote — writers now take a `FOR UPDATE` row lock *before* reading a balance (`finance-locks.ts`); (6) two readers could each post the **opening balance**, invisible to the trial balance because both entries balance — now a **partial unique index** makes one-per-tenant a database fact; (7) the generic **journal reversal** could be pointed at a receipt, withdrawing the money from the books while the allocation stood. Minors fixed too: invoice **lines are fixed once issued** (they were editable with no posting, no step-up, no audit — and the posting-rules doc asserted otherwise), the journal **export is audited + bounded**, `financial.journal.reverse` clearance corrected to 7 to match its permission, defining a period is step-up gated, past-due now outranks part-paid in the derived status, and `heldAsCredit` was renamed `unallocated` (it was the unallocated total at receipt time, not the credit balance held today, displayed next to a card reporting the real one). **New backstops:** migration `20260819160000` adds the opening-entry unique index plus CHECK constraints (a journal line is one-sided and non-negative; money rows are positive; credit never exceeds what was received). **Verify:** api unit **858** (+26 route-guard assertions covering the unauthorized-scope gap the review named, +9 behavioural) · **finance e2e 16/16** (5 new cases pin the fixes, including "cancelling withdraws the charge AND the waiver posted against it, and the books still balance") · **full e2e 33 suites / 240 tests, 0 failures** · `ci:quick` ✔ · `db:rls:check` ✔ · `check:privileged-db` ✔ — and the baseline **improved 28 → 27**, because `FinanceService` no longer needs the privileged client at all. _(claude)_

- `2026-08-19` — **WB5 detailed + built to the ADR-10 Release-1 cut line → WB5-3..5-6 `in-review`.** WB5 had been a one-line "later workbench" with P1/P2 already merged and no slices; it now has a section, an acceptance statement, and eight rows. Built on `feat/wb5-finance-subledger-gl`: (a) **WB5-3** — `Payment` becomes a RECEIPT plus `payment_allocations`, so one payment settles several children's invoices and an invoice collects installments; `payments.invoice_id`/`student_id` **dropped after backfilling allocations**; gap-aware yearly receipt/invoice/journal numbering (`RCT-2026-000042`) replacing the timestamp+random string; audited reprints. (b) **WB5-4** — `account_credits` + `credit_applications`: an overpayment is an AR credit balance (never stored value), drawn down oldest-first when the family's next invoice is issued. (c) **WB5-5** — the **double-entry ledger** (`chart_of_accounts`, `accounting_periods`, `journal_entries`, `journal_lines`): invoice issue, adjustment, receipt and credit application each post a balanced entry through one writer that refuses unbalanced/negative/both-sided lines and anything dated into a closed period; corrections are contra reversals (parity #95), never edits. (d) **WB5-6** — collections (day/method), **aging** buckets, subledger-vs-ledger **control totals**, and a journal **CSV export** (ADR-12: we hand over a file, never take their accounting login). Web: `/finance/payments` rebuilt as receipts + **family checkout** + receipt drawer, new **`/finance/ledger`**, and `/finance/reports` moved off browser-side arithmetic onto the report endpoints (which also drops a legacy `Class` read). **+2 permissions → 354**, **+2 sensitive ops → 34**. **Verify:** `pnpm ci:quick` ✔ · `db:rls:check` ✔ (all 8 new tables) · `check:privileged-db` ✔ (28 grandfathered, unchanged) · `db:verify` **354/11/34** ✔ · api unit **825** · **finance e2e 11/11 on real pg** · **full e2e 33 suites / 235 tests, 0 failures** · an HTTP smoke against the seeded dev tenant (scratch API on :3031) where all three control totals and the trial balance agree to the kobo. Testing found the two real defects on this branch, both now fixed: the opening-balance entry double-counted invoices the ledger had already posted (it now counts only invoices whose charge was never posted, and the books open *before* a bill becomes a receivable), and the cash control compared the ledger against every receipt ever taken, so an upgraded school would have read "needs a look" forever. **Next:** independent review → PR → merge; then the owner decision that unblocks **WB5-7** (gateway provider + signing secret per environment). _(claude)_

- `2026-08-17` — **CORRECTION + fix: the "duplicate sections" were never duplicates — they are two CAMPUSES, and `displayLabel` cannot tell them apart.** An earlier note in this log and the message of commit `afd098b` both called the dev tenant's repeated `JSS2 A` / `SSS1 Science A` sections *duplicate seed data from a non-idempotent seed*. **That was wrong.** `ensureSection` keys on `(tenant, campus, yearLevel, stream, name)` and is idempotent; the tenant simply has **Main Campus** and **Lakeside Annex**, and a two-campus school legitimately runs a "JSS2 A" arm at each (Main holds the offerings and enrolments; the Annex rows are the WB1-6 campus-scoping fixtures). Nine distinct year levels, three distinct campuses, nothing repeated. The wrong call came from reading a listing that showed `displayLabel` and not `campusId`. **The real defect it was hiding:** `composeSectionLabel` builds the stored label from **year level + stream + arm and omits the campus**, so those two sections are byte-identical everywhere a label is shown — which is what made the Economics assessment genuinely ambiguous, and what left the new assessments-workbench picker offering a teacher two entries both reading "SSS1 Science A" with no way to choose. Fixed where it bites: `listTeachableOfferings` now appends the campus **only when two campuses share a section label**, so single-campus schools (the common case) stay uncluttered, and it skips the campus read entirely when there is nothing to disambiguate. `displayLabel` itself is deliberately NOT changed — it is stored, never parsed, and re-composing it would rewrite labels across every tenant to solve a problem only multi-campus tenants have. **Still ambiguous, not fixed:** `describeAssessmentAnchors` labels gradebook rows from the same `displayLabel`; there you are reading a row that already names its subject rather than choosing between two, so it is noise rather than a wrong-choice risk. **Verify:** api unit **791/791** (+2: campus appended only when it disambiguates, and no campus read at all for a single-campus school) · `check-types` + eslint + prettier clean · web `tsc` + eslint `--max-warnings 0` clean. _(claude)_

- `2026-08-17` — **The question bank is re-keyed onto `CurriculumSubject` (owner-directed, stage B tranche 7) — the last thing holding legacy `Course` up on the assessment path is gone.** `Question.courseId` was a required column pointing at the labelled-bag `Course`, and a `SubjectOffering` names a `CurriculumSubject` with **no bridge** to one — so a structured assessment had no bank at all and its paper could not be built. Now `questions.curriculum_subject_id` is the anchor: a bank belongs to a **subject**, which outlives any one course, class or section, which is the whole point of a bank. **Staged exactly like the assessment/grade re-key** — additive nullable column, legacy column kept and merely relaxed, **soft reference with no FK** (curriculum_subjects carries a nullable `tenant_id` for shared national rows, so a cross-schema FK from a tenant-owned table would be wrong; validated in service code, same convention as `SubjectOffering`). Migration `20260817220000_question_curriculum_subject_rekey` + `db:backfill:question-subjects`, which matches the course's free-text subject against subject name/canonicalName/code and **requires a unique match** — anything ambiguous is reported and left for a human, because a bank entry silently attached to the wrong subject surfaces as a physics question in a French exam with no way to trace it. **Service:** create/list/get/update/delete all follow the entry's own anchor through a new `assertCanManageQuestionAnchor`; a teacher's unfiltered bank is narrowed by **both** anchors (`getTaughtCurriculumSubjectIds` added beside the offering/course helpers); `attachQuestions` resolves the assessment's bank from its **offering's subject**, so the refusal added in tranche 6 is gone and a structured assessment's paper can finally be built. **New `GET /questions/subjects`** feeds the picker under `questions.view` — same reasoning as `/assessments/offerings`: `/academics/structure/offerable-subjects` needs `academics.structure.view`, which teachers do not hold. It returns **one row per subject**, not per offering — a subject offered to four sections is still one bank. **Web:** the question-bank workbench picks a **subject** instead of a course, and the assessments workbench loads its bank from the selected offering's subject, so the "question paper unavailable" banner is removed. **Verify:** api unit **789/789** (+5, plus two tranche-6 tests rewritten because they pinned the temporary refusal that this removes) · `check-types` + eslint + prettier clean · web `tsc` + eslint `--max-warnings 0` clean · `check:legacy-readers` steady at 50. **Applied on the dev database:** migration in via `db execute` + `migrate resolve --applied` (never `migrate dev` — checksum drift resets), then the backfill; **all 14 bank entries re-keyed, 0 needing a human.** `db:check:legacy-retirement` now reports **no data blockers at all** — the first time it has been green — after the assessment side was finished too (3 recovered by the matcher fix below, 1 hand-attached to the Economics offering whose section actually holds an enrolment). Gates re-run green end to end: `ci:quick`, `db:verify` 8/8, `db:rls:check`, api unit 789/789, `check:legacy-readers` 50. _(claude)_

- `2026-08-17` — **The assessments WORKBENCH is re-keyed onto the offering (owner-directed, stage B tranche 6) — and the question bank is now the one thing still holding the legacy `Course` up.** `classes/assessments` was class-picker-driven end to end; it now picks **section × subject**, lists by `subjectOfferingId`, and **creates against the offering** (the API copies year and term off it). **New endpoint `GET /assessments/offerings`** feeds the picker — deliberately served from the assessments controller rather than pointing the workbench at `/academics/structure/offerings`, which requires `academics.structure.view`: a teacher holds `assessments.view`, so the registrar endpoint would have 403'd exactly the people who need the picker. It is narrowed to the actor's active `OfferingTeacher` rows, mirroring how the old class picker was already narrowed. **Second silent 404 found and fixed:** `QuestionBankService.getManagedAssessment` filtered `where: { id, class: { academicYear: { tenantId } } }` — a relation filter is a NOT-NULL test, so **every question-paper operation on a structured assessment answered "Assessment not found"** while the row sat right there; now scoped through `academicYear` and guarded by the assessment's own anchor. **THE REMAINING BLOCKER, and it is a schema decision, not a UI one:** `Question.courseId` is a **required column** pointing at the legacy `Course`, and a `SubjectOffering` names a `CurriculumSubject` with **no bridge to a Course** — so a structured assessment has no bank to draw from. `attachQuestions` now **refuses with a plain message** instead of dereferencing `assessment.class.courseId` on a null class, and the workbench says so in place rather than showing an empty picker. Everything else about a structured assessment works: create, list, publish, submissions, grading. Re-keying `Question` onto `CurriculumSubject` (schema + migration + backfill, the item already on this board as needing an owner decision) is what closes it — and it is now the **last** thing keeping `Course` alive on this path. *(done in the next entry.)* **Verify:** api unit **784/784** (+7: teacher-scoped vs manage-all picker, and a new `question-bank-anchor.spec.ts` pinning the year-scoped lookup, the offering guard, the clear refusal, a genuine 404 still 404-ing, and the legacy course bank still working) · `check-types` + eslint + prettier clean · web `tsc` + eslint `--max-warnings 0` clean · `check:legacy-readers` steady at 50 · authenticated visual pass still owner-gated, so no browser render check. _(claude)_

- `2026-08-17` — **A teacher's assessment list omitted every structured assessment; the `/assessments` family now follows the anchor (stage B, tranche 5).** `listAssessments` narrowed non-admins with `classId: { in: taughtClassIds }`, so an offering-anchored assessment — which has no class — could not appear: the work a teacher had actually been assigned was **absent from their own list**, with no error. It now asks **both** questions (`subjectOfferingId in taught offerings` OR `subjectOfferingId null AND classId in taught classes`), and accepts a **`subjectOfferingId` filter** that takes precedence over the deprecated `classId` one and is guarded by the offering rule. `getAssessment`, `updateAssessment` and `deleteAssessment` moved onto the shared `assertCanManageAssessmentAnchor` — all three refused every teacher on a structured assessment, so one could be created and then **neither opened, edited nor deleted**. **Labels:** a new `describeAssessmentAnchors` resolves `subjectLabel` + `classLabel` + `anchor` per row from whichever anchor is present (the offering is a soft reference, so nothing followed it automatically), which is what stops the gradebook and standing pages rendering structured assessments as **"Unassigned"**; `AssessmentSummary` in `apps/web/lib/academics.ts` now declares `classId` **nullable** beside `subjectOfferingId`, so the next reader that assumes a class gets a type error rather than a blank cell. **Two pre-existing bugs found in passing and fixed:** both `classes/gradebook` and `reports/academic` read `assessment.title`, a field that **does not exist on the model** (it is `name`) — the gradebook's Assessment column had been rendering a raw **UUID**, and the analytics label silently fell back to the class name. **Verify:** api unit **777/777** (+8: both-anchor narrowing, explicit-offering filter + guard, manage-all unnarrowed, offering and legacy labelling, and get/update/delete each through the offering guard) · `check-types` + eslint + prettier clean · web `tsc` + eslint `--max-warnings 0` clean · `check:legacy-readers` steady at 50. **DEFERRED, needs an owner decision:** the `classes/assessments` **workbench** is class-picker-driven end to end — it lists `/classes`, filters on `assessment.classId === classId`, and creates against a class — so it still cannot show or author a structured assessment. Re-keying it means the picker becomes section × subject (offering), which is a WB2 workbench flip in its own right, not a filter change. *(owner chose the full re-key; done in the next entry.)* _(claude)_

- `2026-08-17` — **The assessment gradebook could not name a student on a structured grade — and a teacher could not open one at all (stage B, tranche 4).** `listGradesForAssessment` reached the student through `enrollment.student`, which is null on every structured grade, so the three surfaces that render `/grades/assessment/:id` showed **"Unknown student"** with a blank student number. Worse, its access check asked `assertCanManageClass` with a **null `classId`** — the same trap fixed for `createGrade` in `1d7046b` — so a teacher was refused outright on any offering-anchored assessment; only a manage-all admin could see the endpoint at all. Both fixed: a new `assertCanManageAssessmentAnchor` follows the assessment's own anchor (offering, else class) and is now the single answer to that question across `createGrade`, `updateGrade`, `listGradesForAssessment` and `getAssessmentAnalytics` — `updateGrade` and the analytics endpoint carried the identical refusal, so **a mark on a structured assessment could be entered but never corrected**. The student is resolved by a dedicated read (`Grade.studentId` has a DB foreign key but no Prisma relation) from whichever anchor the row carries, and surfaced **flat on each row** as `student { id, studentNumber, firstName, lastName, email }` plus a normalised `studentId`. **Web moved with it** — this endpoint, unlike the report card, has three real consumers: `classes/gradebook` and `students/gradebook/standing` (name + student number), and `reports/academic`, whose "students assessed" headline counted **distinct `enrollmentId`s** and so silently **undercounted** by every structured grade. Standing also grouped rows on `enrollmentId`, which split one child's record across their enrolments; it now groups on the student. **Verify:** api unit **769/769** (+5) · web `tsc` + eslint `--max-warnings 0` clean · `check:legacy-readers` steady at 50 · authenticated visual pass remains owner-gated (credential guardrail), so the render check is types + tests only. **Still open, same defect class, bigger flip:** the `/assessments` family (`getAssessment`, `updateAssessment`, `deleteAssessment`) asks the same class-only question, and `listAssessments` filters non-admins by `classId: { in: taughtClassIds }` — so **a teacher's assessment list omits every structured assessment**, which also leaves the two gradebook pages labelling them "Unassigned". *(fixed in the next entry.)* _(claude)_

- `2026-08-17` — **The documented grade-report gap is FIXED: a student's report is now read by student and grouped by subject (stage B, tranche 3). 51 → 50 legacy call sites.** `getStudentReportCard` started from legacy `Enrollment` rows, so every grade on a **structured** assessment — `studentId` set, `enrollmentId` null — was missing from the child's record with nothing to signal it. Re-shaped rather than re-keyed, exactly as the standing comment said it had to be: the read starts from the **student**, rows group by the assessment's own anchor (`subjectOfferingId`, falling back to `classId` for rows the backfill has not reached), the term comes from that anchor instead of `enrollment.class.termId`, and the teacher filter asks the **offering** question via a new `AcademicsAccessService.getTaughtOfferingIds`. **The class arm is deliberately kept** in the scope filter: a legacy assessment still belongs to a class, so scoping *only* by offering would have hidden legacy marks from the teacher who recorded them — one silent omission traded for another. Likewise the where-clause keeps an `enrollment.studentId` arm, because `db:backfill:assessment-offerings` is operator-run and a student-only read would drop every un-backfilled row in any environment where it has not run yet; both arms come out with the column. **Contract changed** (`enrollments[]` → `subjects[]`, each carrying `anchor`/`subjectLabel`/`classLabel`/`termId`; `summary.coursePercentage|courseGpa` → `summary.percentage|gpa`) — safe to land in one commit because `GET /grades/student/:id` and `POST /grades/student/:id/report-card` have **no consumer in `apps/web`**: the report-cards route was retired into Gradebook standing, which reads `/assessments` + `/grades/assessment/:id` instead. `listGradesForStudent` had the identical enrolment-first defect and moved with it. **Still open, same defect class:** `listGradesForAssessment` names the student only through `enrollment.student`, so a structured grade shows a blank name on the assessment gradebook. *(fixed in the next entry.)* **Verify:** api unit **764/764** (+8 in `student-grade-report.spec.ts`, incl. a structured grade appearing at all, offering/legacy grouping, year-long-offering term fallback and the two scope arms) · `check-types` + eslint + prettier clean · `check:legacy-readers` **51 → 50**, baseline re-locked. _(claude)_

- `2026-08-17` — **Retirement stage B in progress: 56 → 51 legacy call sites; two silent defects found and one documented.** Flipped so far: **search** (now indexes `ClassSection` on its composed `displayLabel` + year level + stream, so it finds the class a school actually runs), **analytics + overview counts** (classes, teaching load, a student's enrolments — all three were quietly WRONG, not merely old), and **lesson authoring** (`createLesson` accepts a curriculum subject, so new lessons land in the library). Also consolidated the offering/subject access rule into `AcademicsAccessService` — steps 2 and 3 had each grown their own copy, and three answers to one authorisation question is how a hole gets in. **Defect found + fixed:** once assessments could anchor to an offering, **grading one was impossible** — `createGrade` still asked the class-teacher question with a null `classId`, refusing every teacher; now it follows the assessment's own anchor and resolves the student from either route. **Defect found + DOCUMENTED, not fixed:** `getStudentGrades` starts from legacy `Enrollment` rows, so grades on structured assessments are **silently missing** from a student's grade report; the fix is a re-shape of query AND response contract (group by offering, aggregate terms from it, update web consumers), so it must move with its callers — commented in place and tracked. **Guard shipped:** `check:legacy-readers`, a ratchet on the privileged-db pattern, fails when any file's legacy call-site count rises (verified it fires). Of the 51 remaining, **38 get DELETED not flipped** (34 in `academic-structure.service` + 4 legacy enrolment CRUD in `student.service`, both duplicating WB2-3 lifecycle), 6 are the class-based helpers in `academics-access` (last, once nothing calls them), and **7 are genuine flips** — of which attendance (keyed on class end to end), question-bank (scoped to legacy `Course`) and people-directory (profile panel reshapes with `SectionEnrollment`) need an owner decision first. _(claude)_

- `2026-08-17` — **Legacy retirement STAGED, not executed — a drop today would destroy data.** Measured the real dependence before touching anything: **4 of 10 assessments still have no `subject_offering_id`** and (before this change) **8 lessons had no `curriculum_subject_id`**, while `assessments.class_id` AND `lessons.class_id` are both **ON DELETE CASCADE**. Dropping `Class` in that state silently deletes those assessments with their grades, and those lessons with their materials and embeddings. **~11 API services still read the legacy models** (attendance, learning, student, reporting-analytics, search, assessment-grading, overview, academic-structure, people-directory, question-bank, academics-access). So retirement is a programme with three stages, not one migration: **(A)** re-key the data — done for grades/submissions (deterministic) and now lessons; **(B)** flip every code reader onto the structured anchors — NOT started, the largest remaining piece; **(C)** drop the tables — only when A and B are clean, in every environment separately. Shipped this slice: **`db:check:legacy-retirement`**, a readiness gate that reports exactly what would be destroyed and **exits non-zero** so it can gate a deploy (it checks DATA only — code readers must be confirmed separately); and **`db:backfill:lesson-library`**, which moved all 8 legacy lessons into the library (8 matched, 0 ambiguous, idempotent on re-run). Gate now reports **1 remaining data blocker**: the 4 unmatched assessments, which sit in an academic year with no offerings and need a human to create the offerings or attach them. _(claude)_

- `2026-08-17` — **Cross-repo ALIGNMENT assessment + 4 owner decisions; a new WB12 programme is implied.** Read two sibling school systems end to end — `learn-lift-backend/src` (17 Mongoose schemas) and `ewosoft-tech-gau-dashboard-api/src` (11) — against our 135 Prisma models. **Headline finding:** every content + assessment consumer here is still keyed on the **legacy `Class`** (`Lesson.classId`, `Assessment.classId`, `Enrollment.classId`) while WB2/WB4 moved enrolment, placement, promotion and results onto the structured ADR-02 spine (`ClassSection`/`SubjectOffering`) — so the Classes and Academics menus are two generations of one model, which is exactly why `Classes → Subjects` (legacy `Course`) could not be retired and why the boundary reads as arbitrary. `Lesson` has **no subject reference at all**. Also found: **two file systems** (`LessonMaterial.storageKey` bypasses F4 `Document`, losing scan/retention/versioning) and `Document.thumbnailKey` present but read by **no UI** — that, not view code, is what blocks a list/grid tile toggle for students. What the references have that we lack: a **fixed Nigerian level taxonomy** (LearnLift hardcodes PRY*1..6/JSS/SSS/L_100..600; **no nursery**), `Faculty`→`Department` for tertiary, a `Chapter` grouping between subject and lesson, and a thumbnail on every entity. What we already do better: campus + **arms** (`Stream`) + tenant-composed labels (LearnLift has none of these), F4 document governance, and AI retrieval (`MaterialChunk.embedding` behind `AIMediatorService` vs GAU persisting `Course.assistantId` and delegating retrieval to OpenAI Assistants — borrow only its **scoping**: context bound to the teaching unit, thread per user+unit). **OWNER DECISIONS:** (1) level spine = add fixed `levelCode` to `YearLevel` + `educationLevel` to `Stage`, tenant naming on top, **plus** arm description/aliases on `Stream` — nursery included, no WB2 rebuild; (2) lesson ownership = **BOTH** a curriculum-level library **and** per-section instances; (3) re-key legacy `Assessment`/`Grade` onto `SubjectOffering` as **its own workbench**, then retire `Class`/`Course`/`Enrollment`/`ClassTeacher` + the Course-catalogue page together; (4) **tertiary deferred** — K-12 first, `TERTIARY` stays in the enum but no Faculty/Department, and LearnLift's polymorphic-Subject variant fields are not copied. Assessment doc: artifact `75839cee-ffbd-444d-83ed-66086125376d`. *(claude)\_

- `2026-08-17` — **WB4-2 + WB4-3 + WB4-4 built to DoD → `in-review`; Workbench-4 complete pending review.** Branch `feat/wb4-import-traits-transcript` (base `main` @ `d515a14`). **WB4-2 · spreadsheet import (job #54):** `ResultImportService` — a pre-filled CSV **template** per section, then a **dry run by default** that names every unmatched row and unreadable cell, with a **commit refused while any error stands** (a typo can never land as a silent 0). `.csv` reuses the **F2 `parseCsv`**; `.xlsx` goes through **exceljs** (already an `apps/api` dependency), walking the row width so a blank cell stays a blank column. The commit path calls **`ResultEntryService.upsertEntries`**, so the cycle-open gate, in-scope (student · offering) check, per-component max and audit entry are identical to keyed entry — one owner for entry writes. **Absent ≠ zero survives the round trip:** `ABS`→absent, `EXM`→exempt, **blank→no row at all**; a duplicated (student · subject · component) row is refused rather than last-write-wins. **WB4-3 · affective/psychomotor traits:** `ResultTrait` + `ResultTraitRating` + an additive `published_student_results.traits` JSONB (migration `20260817120000_wb4_result_traits`, own+platform RLS + `app_runtime` grants). The rubric is **draft-only** (ratings can never be stranded) and an **unrated trait is absent from the snapshot** — never published as the lowest rating. Publish snapshots the ratings **and the rubric** (so a report card still renders labels + scales later), with rubric order driving serialisation because the publication **checksum** depends on it; the report card gained a per-domain behavioural table. **WB4-4 · cumulative transcript (jobs #63/#66):** `ResultTranscriptService` assembles a student's record from **non-superseded `PublishedStudentResult` rows only** — never the live gradebook — citing each term's publication **version + snapshot checksum**, campus-filtered by the reader's WB1-6 grant scope, with a subject-weighted cumulative average that **excludes** absent/exempt subjects instead of zeroing them; `issueTranscript` stores an immutable, audited `Student`-owned **F4 artifact**. To keep "one fact, one owner" true, the legacy gradebook-derived page was **renamed to "Gradebook standing"** and linked to the real transcript rather than left competing for the name. **Web:** import panel under the entry grid (template → check → import, commit disabled until clean), a **Behaviour** tab (rubric editor with a standard-rubric starter + rating grid), and a cycle-independent **Transcripts** card. **Zero new permissions — stays 352 / 32 sensitive-ops**; no privileged client. **Verify:** api unit **46/46** in the results module (+33 new across `result-import.spec.ts`, `result-transcript.spec.ts`, `result-artifact.spec.ts`) · **results e2e 25/25 on real pg** across both suites (the new 16 cover dry-run-writes-nothing, ABS/blank/EXM semantics, a real `.xlsx` workbook, template self-round-trip, draft-only rubric + over-scale refusal, traits snapshotted with the unrated one omitted, transcript excluding a superseded version and an absent subject, audited artifact issue, RLS on both new tables + 401) · `check:privileged-db` ✔ · `db:rls:check` ✔ · `db:verify` 352/32 ✔ · `ci:quick` ✔ · web `tsc` + eslint `--max-warnings 0` ✔ · prettier ✔. One real bug caught by the tests before it shipped: the template labels columns `"First CA (max 20)"`, so header normalisation had to strip a trailing parenthetical or the importer could not read its own template. Authenticated visual pass remains **owner-gated** (credential guardrail); both touched routes render + 307 to `/login` with no console/server errors. **Fast-follows (not blocking):** PDF-binary render on the existing artifact seam; pointing (or retiring) the gradebook standing page's data at the snapshot endpoint; a searchable student picker past the shared `/students?limit=100` cap; trait ratings via the same import flow. _(claude)_

- `2026-08-17` — **Board reconcile: WB4 gets its own section; WB4-1 recorded `done` (was never on the board); WB4-2/3/4 claimed (claude).** The **WB4 ResultCycle spine merged on 2026-08-08 as [PR #83](https://github.com/Ewosoft-Solutions/claude-trial/pull/83) → `2bc552b`** (ADR-04: 10 result tables, configure→enter→validate/moderate→maker-checker publish→immutable checksum snapshot→amend/supersede, `FinancialHold`, report-card + broadsheet artifacts on the F3 job substrate, promotion recommendation, ranking-as-policy default-off, **+5 permissions → 352**, `/academics/results` workbench) but **no board row or `AI_HANDOFF.md` entry was ever written for it** — WB4 still read as untouched in "Later workbenches". Corrected: a Workbench-4 section now carries WB4-1 `done` plus the three parity gaps that survived that build, claimed on branch `feat/wb4-import-traits-transcript`: **WB4-2** bulk CSV/XLSX score import (job **#54** — "direct entry **+ Excel import** in one flow"; the merged build has only a best-effort name-matching gradebook seed), **WB4-3** affective/psychomotor trait rubric (Nigerian report-card standard; called out in [`BACKLOG.md`](BACKLOG.md) §2D and [plan 05](../plan/05-academic-nigeria-international.md)), **WB4-4** cumulative transcript from published snapshots (job **#63**/**#66** remainder — today's `/students/gradebook/transcripts` derives from the _mutable_ gradebook, so a transcript cannot be reproduced). No new permissions planned (all three reuse `academics.results.view`/`.enter`/`.manage`), so the count stays **352**. _(claude)_

- `2026-08-14` — **WB3-5 (admission fee/deposit → link Finance) built; WB3 COMPLETE (WB3-1..3-5 all done).** Branch `feat/wb3-5-admission-fee-finance` (base `main`). Admission `fee` requirements (application/acceptance fee) stop being a bare "mark paid" toggle and become **real Finance AR records**: a **studentless `FeeInvoice`** keyed to the application (owner decision A: _bill via Finance now, re-key on convert_), settled with a real `Payment`, and on `convertToStudent` the application's invoices/payments are **re-keyed to the new student** (AR history follows into the student ledger with zero re-billing). New **deposit gate** (owner decision B: _block until settled, waivable_) — `convertToStudent` refuses while a required `fee` requirement is still `pending`; staff waive to override. Schema (additive migration `20260814000000`-class `20260814120000_admission_fee_finance_link`): `fee_invoices.student_id`/`payments.student_id` → **nullable**, `fee_invoices.admission_application_id` added + index; **no RLS change** (policies key off `tenant_id`). New `AdmissionFeeService` (bill/settle) delegates all finance writes to `FinanceService.createAdmissionInvoice`/`settleAdmissionInvoice`/`reassignAdmissionInvoices` + `FinanceCatalogueService.ensureFeeItem` (`admission:<key>` items) — one source of truth for invoice/payment writes; `provideRequirement` now rejects `fee` type. Web: bill + record-payment inline controls on the requirement checklist. **Zero new permissions (stays 352)**; **no privileged client**. Verify: admissions **e2e 23/23** real-pg (added bill→invoice-shape, settle→provided, gate-blocks, settle+waive→convert→re-key) + finance unit 22/22 no-regression · check-types (api+web) · eslint (0) · prettier · `check:privileged-db` · `db:rls:check` · `db:verify` (352/32) all green · **live browser walkthrough on `sunrise-primary`** (bill ₦5,000 → record payment → `provided`; invoice shows in Finance AR list `Paid`). Out of scope (follow-ups): offer/accept letters + F5 notifications; portal self-pay; a finance `kind` discriminator. Also flagged a **pre-existing** finance-invoices date-hydration bug (Intl.DateTimeFormat without a pinned timeZone) — unrelated to WB3-5. _(claude)_

- `2026-08-06` — **Board reconcile (state check): WB3 structured-intake MERGED; finance P1 landed; permissions now 347.** Correcting the status framing of the entry below: the WB3 structured-intake redesign is **merged as [PR #78](https://github.com/Ewosoft-Solutions/claude-trial/pull/78) → `9462f2c`** (not "in-review"), with a follow-up test fix **[#79](https://github.com/Ewosoft-Solutions/claude-trial/pull/79) → `62dc667`** (self-provision `access.grants.manage` in the step-up e2e) on top — `62dc667` is current `main` HEAD. Also confirmed on `main` since the WB2/WB3-core merges: **finance WB5 P1** — server-driven lists + billing/AR + fee-item catalogue (**[#76](https://github.com/Ewosoft-Solutions/claude-trial/pull/76) `a66cb3c`** + refactor **[#77](https://github.com/Ewosoft-Solutions/claude-trial/pull/77) `1245e54`**), plus **[#70](https://github.com/Ewosoft-Solutions/claude-trial/pull/70) `91f8caa`** (WB1-6 HTTP step-up boundary test) and **[#74](https://github.com/Ewosoft-Solutions/claude-trial/pull/74)/[#75](https://github.com/Ewosoft-Solutions/claude-trial/pull/75)** (students 4xx surfacing / dev launch-config rename). **Authoritative permission count is now 347** (seed `EXPECTED_PERMISSION_COUNTS.total`: the 339 this session left + WB2-3 lifecycle `+2` + WB2-4 promotion `+3` + WB3 `admissions.convert` `+1` + finance `finance.view`/`.manage` `+2`). Board task rows already reflect **WB2-1..2-4 + WB3-1/2 `done`**; the "▶ Ready to claim" header is refreshed to match. Verified: `git merge-base --is-ancestor 9462f2c origin/main` = true; local `main` == `origin/main` == `62dc667`. _(claude)_

- `2026-08-06` — **WB3 structured-intake redesign → in-review (PR open).** Branch `feat/wb3-admissions-structured-intake` (rebased on `main`). Owner-driven redesign so a school keeps **no parallel admissions list** and an admit lands in a real class with **zero re-keying**: (a) `createApplication` is now a **cascade** over the WB2-1 academic structure (class = `YearLevel`, its level = `Stage`, optional department = `Stream`, optional `Campus`) — validated against the tenant's own structure, with the stored `applyingFor` label **COMPOSED** from it (never parsed); captures the applicant profile (DOB/gender/state/religion/health) + a **multi-guardian** block (`AdmissionGuardian`: relationship, phone + WhatsApp with a same-as-phone reuse, exactly-one-primary enforced server-side). (b) A **configurable requirements framework** — `AdmissionRequirement` (per-tenant editable template: document/field/measurement/fee tagged to a **collection stage** = application/offer/acceptance/enrolment) **snapshotted** onto each application as `AdmissionApplicationRequirement` (FK **RESTRICT** so a template delete can't wipe in-flight fulfilments), then **provided / uploaded (F4 → R2) / waived**; default Nigerian checklist seeded on demand. (c) **Cloudflare R2** S3-compatible `StorageProvider` (aws4fetch SigV4; auto-selected when the four `R2_*` env vars are set, else local disk) — **verified round-trip against the real bucket**; env parity (`env.config.ts` + `.env.example` + `render.yaml`, replacing the stale `STORAGE_S3_*` placeholders); body-parser raised to 20mb so real document uploads don't 413. (d) **Web rebuild:** full-width Applications table + search, structured **New Application** cascade form in a side sheet, at-a-glance **drawer** on row-click, and a full **`/admissions/[id]`** detail/edit page (staged requirement checklist with upload/measure/fee/waive, history, reviews, decisions). New migration `20260806010000_admissions_structured_intake` (3 tables + RLS + FKs). **Reuses existing admissions permissions** (create/view/documents/criteria) — **no permission-count change (stays 345)**; **no privileged client**. Independent review returned **all gates green + 10 findings (1 High/2 Med/rest Low/Nit), all fixed** (silent convert-picker empty-state, upload size cap, FK RESTRICT, guardian single-primary, intake-structure gated `.view`, a11y rows, inline waive). api unit **12** (admissions+storage) · **admissions e2e 14/14 on real pg** (structured create, guardians, checklist instantiation, provide/upload[local-disk]/waive, + the WB3-1/2 pipeline) · check-types (api+web) · eslint · `check:privileged-db` · `db:rls:check` all green. Extends the WB3-1/2 core toward WB3-3 (structured form) territory. RESUME: owner browser test → merge on green CI. _(claude)_
- `2026-08-06` — **WB3-1 + WB3-2 → `done` (merged); WB3 admissions core complete.** [PR #73](https://github.com/Ewosoft-Solutions/claude-trial/pull/73) squash-merged to `main` as `5a7dd00`, **CI green** ("Build, Lint, Type-check, Test" 6m22s — isolated `next build`/`nest build` + `db:deploy` applying migration `20260805060000` + `db:rls:check` + `check:privileged-db` all passed). Replaces the flat page-first `AdmissionApplication` stub (strings-only stage/decision + a grandfathered privileged-client read — **removed**, baseline 29→28) with a durable, Person-linked pipeline: **WB3-1** stage machine (enquiry→applied→screening→interview→offer→accepted→enrolled, +rejected/withdrawn) with effective-dated `AdmissionStageEvent` history + scored `AdmissionReview` decision history (fixes matrix job 17 "strings only"); **WB3-2** offer/accept/reject transitions + **`convertToStudent`** = the one-command headline (job 23): from an ACCEPTED application, create Person (F1) + login-less profile (User+UserTenant) + `Student` (number via the WB2-3 allocator) and **register into a section via the WB2-3 `StudentLifecycleService`**, one tx, audited, campus-scoped, idempotent. **+1 permission** `admissions.convert` (344→**345**; offer/accept reuse `admissions.approve`, reject reuses `admissions.reject`). Web `/admissions` pipeline + catch-all proxy + nav entry. Independent maker-checker review returned **CHANGES-REQUESTED → 1 major (advanceStage lacked source-state validation → a clearance-review actor could regress/undo a higher-clearance offer/reject) + 5 minors, all fixed → APPROVE** (confirmation pass verified the diff; the fix added `ADVANCE_SOURCE_STAGES`/`OFFERABLE_FROM_STAGES` + a proving regression e2e). api unit **593** · **e2e 9/9 on real pg**. **WB3 remaining slices** (WB3-3 versioned form, WB3-4 interviews+quiz, WB3-5 fee[blocked on WB5]) are detailed-but-backlog in [`workbench-admissions.md`](workbench-admissions.md). _(claude)_

- `2026-08-05` — **WB2-3 + WB2-4 → `done` (merged); WB2 COMPLETE (WB2-1..WB2-4 all done).** [PR #72](https://github.com/Ewosoft-Solutions/claude-trial/pull/72) squash-merged to `main` as `33a6d5d`, **CI green** ("Build, Lint, Type-check, Test" 6m3s — the isolated `next build`/`nest build` + `db:deploy` applying migration `20260805050000` + `db:rls:check` + `check:privileged-db` all passed) — gate met (CI-green **+** merged), so **WB2-3 (student lifecycle) + WB2-4 (promotion workbench) flip `done`.** With them, **Workbench-2 is complete**: the structured ADR-02 model (WB2-1), enrollment/registration/electives/teacher-assignment (WB2-2), the durable effective-dated student lifecycle (WB2-3), and the maker-checker promotion rollover (WB2-4) — all five workbench-2 acceptance scenarios covered. Independent maker-checker review returned **CHANGES-REQUESTED → 8 findings, all fixed → APPROVE** (the reviewer also re-ran the whole suite itself: api unit 590, e2e 11/11, gates green). Owner did a live browser pass (both surfaces render + the promotion preview/exception flow works on real data); two browser-found fixes shipped in the same PR — the academics student picker was empty because `/students?limit=200` exceeds the shared `PaginationDto` `@Max(100)` cap (→ 400 → `serverApiGet` null → empty), lowered to 100 (the broader finance/analytics `limit=500/1000` callers hitting the same cap are a separate follow-up); and the lifecycle view now surfaces a pre-lifecycle student's current section from their WB2-2 enrollment. **344 permissions / 32 sensitive-ops.** Next academic-facing work builds on this substrate: **WB3 (admissions)** converts applicants into registered students via the WB2-3 lifecycle; **WB4 (results)** references the WB2 offerings + consumes WB2-4 promotion input; **WB8 (daily-work)** hangs the class workspace on the offerings. _(claude)_

- `2026-08-05` — **WB2-3 + WB2-4 independent review → APPROVE (after fixes); WB2 green, awaiting owner test + merge.** An independent maker-checker review returned **CHANGES-REQUESTED** with 8 findings; all fixed on the branch (commit `b12bbab` + a follow-up `cancelRun` expiry-robustness tweak): **(major)** the promotion `repeat` exception silently PROMOTED the student (setException left the preview's next-level proposal in place) — now cleared so the commit resolver falls back to the source section, proven by a new e2e (a repeat lands in the SS1 source section next year); **(major)** a manual promotion exception could place onto another campus — now scope- + run-campus-checked; **(major)** promotion-run reads (`listRuns`/`getRun`) weren't campus-scoped — now clamped/asserted (mirrors the WB2-1 read clamp); **(minor)** lifecycle reads (`explainPlacement`/`listPlacementHistory`) now campus-scoped to the student's current campus; **(minor)** withdraw/graduate now assert scope on EVERY active-enrollment campus (a student may span campuses post-promotion); **(minor)** added a `cancel` path (rejects any pending maker-checker request — best-effort even if expired — and sets `cancelled`) + Cancel button, so a submitted run is never stuck; **(nits)** clearer transfer-back error + deterministic preview ordering. The **confirmation pass returned APPROVE** (all 8 resolved, no new defects; two narrow non-blocking residuals noted). Re-validated: api check-types + web tsc + api/web lint + prettier + `check:privileged-db` + `db:rls:check` green · api unit **590** · **e2e 11/11 on real pg** (repeat now asserted). Rows stay `in-review` until merged (gate = CI-green + merged). _(claude)_

- `2026-08-05` — **WB2-3 + WB2-4 built to DoD → `in-review` (claude); completes WB2 pending review.** One session, one branch `feat/wb2-3-4-lifecycle-promotion` (owner asked to finish a whole workbench per session). **WB2-3 — student lifecycle:** a new durable, effective-dated ledger **`StudentPlacementHistory`** (span per (campus·section·year)) + **`StudentLifecycleService`** (register · transfer · withdraw · graduate · `explainPlacement`) — the ONE authoritative writer that keeps the WB2-2 `SectionEnrollment`, the ledger, and `Student.enrollmentStatus` in lock-step per `@TenantScoped` tx, audited, campus-scoped (WB1-6 `AccessScopeService`). A transfer CLOSES the current span (keeps it, with an end date) + OPENS a new one; withdrawal/graduation flip status + close spans — **prior placements are never destroyed** (scenarios 3+5). Identifier allocation via a pure `nextStudentNumber` (`STU-<year>-NNNN`, never a label parse); credential issue reuses WB1-3 provisioning (not rebuilt). Controller `/academics/lifecycle/*`. **WB2-4 — promotion workbench:** **`PromotionRun` + `PromotionRunItem`** + **`PromotionService`** (createRun → **preview** the cohort with proposed next-year sections matched by (stream,name) → per-student **exception** repeat/withhold/manual, changes only that item → **requestCommit** raises a WB1-6 **maker-checker** (`academics.promotion.commit`, maker ≠ checker, clr 7) → **approveAndCommit** creates NEXT-year `SectionEnrollment`s + 'promotion' spans via the WB2-3 lifecycle and **leaves the prior year untouched**, scenario 4). Commit is synchronous+transactional; routing multi-thousand-row commits through an F3 job is the documented scale path (mirrors F2-fu2). Controller `/academics/promotion/*`. (a) **3 new tenant-owned tables** (own+platform RLS, `db:rls:check` green; migration `20260805050000`, DB-level FKs) — `student_placement_history`, `promotion_runs`, `promotion_run_items`. (b) **+5 permissions** → 339→**344** (`academics.lifecycle.view`/`.manage` + `academics.promotion.view`/`.manage`/`.approve`; `EXPECTED_PERMISSION_COUNTS` + `ACADEMIC_MANAGEMENT_PERMISSIONS` 25→30 updated) + **1 sensitive op** (32). (c) **Web**: `/academics/lifecycle` (student picker → placement + history timeline + register/transfer/withdraw/graduate) and `/academics/promotion` (F8 `ApprovalPanel`; create run → preview → per-student decision + manual placement → submit → approve) + dedicated proxies + 2 nav entries. **Validation:** api check-types + web tsc green · api lint (new/edited files) clean · web lint `--max-warnings 0` clean · prettier (changed files) clean · `check:privileged-db` green (TenantDbService-only) · `db:rls:check` green (3 new tables) · `db:verify` **344 perms / 11 pools / 32 sensitive-ops** (the pre-existing "Platform Bootstrap" verify item fails locally only — the seed creates the Architect with `passwordHash: null` by design; unrelated to this change) · api unit **590** (+8: lifecycle 4, promotion 4) · web unit **138** · **e2e 11/11 on real pg** (WB2-3 6: register→first span · transfer keeps both placements w/ dates · withdrawal flips+preserves · graduation · campus-scope deny/allow · RLS+401; WB2-4 5: preview cohort+proposals · exception changes only that student · maker can't self-approve · second-approver commits→next-year rows created + prior year untouched · RLS+401). Isolated `next build`/`nest build` **deferred to CI** (dev servers live — shared-`.next`/`dist` gotcha); web routes verified to compile (307→/login); authenticated visual pass owner-gated (credential guardrail). **Next:** independent review → apply fixes → green → owner tests → merge → WB2-3+WB2-4 `done` = **WB2 complete**. _(claude)_

- `2026-08-05` — **WB2-2 → `done` (merged); WB2-3 → `ready`.** [PR #71](https://github.com/Ewosoft-Solutions/claude-trial/pull/71) squash-merged to `main` as `360c21c`, **CI green** — gate met (CI-green **+** merged), so WB2-2 flips `done`. Note: the owner's follow-up `b3a7ebf` on the branch (campus-scope clamp on the WB2-1 list reads + a 409-on-rename guard + a `db:seed:structure` dev seed) landed unformatted and tripped a **newly-added `Format check (changed files)` CI step**; fixed with a prettier-only commit (verified: typecheck/lint/`db:verify` 339/`pnpm audit` clean/**e2e 16/16** incl. the new read-clamp test). With WB2-2 on `main`, **WB2-3** (student lifecycle: registration · transfer · withdrawal · graduation, durable effective-dated history) flips `backlog → ready` — deps WB2-2 + F1 both `done`. _(claude)_

- `2026-08-05` — **WB2-2 built to DoD → `in-review`.** Branch `feat/wb2-2-enrollment`. Joins students to what they study, the way the tenant's profile demands — **additive** over the legacy `Enrollment`(→Class)/`ClassTeacher`. Delivered: (a) **5 new tenant-owned tables** (own+platform RLS, `db:rls:check` green; migration `20260805040000`, DB-level FKs) — `academic_profiles` (`enrollmentModel` 'class'|'course', effective-dated, default derived from `Tenant.schoolType` when absent), `section_enrollments` (K-12: Student→`ClassSection`), `course_registrations` (tertiary: Student→`SubjectOffering`), `student_subject_elections` (electives reference an OFFERING, not free text), `offering_teachers` (teacher→offering, **not a label**). (b) **`EnrollmentService` + controller** at `/academics/enrollment/*` (`TenantDbService`-only — `check:privileged-db` green; permission→validation→mutation→**audit**→state; **campus-scoped** via the WB1-6 `AccessScopeService`) with a **`resolveStudentSubjects`** resolver that picks class-vs-course by the active profile and resolves student→subjects **through offerings** (core section offerings + elected offerings for K-12; registrations for tertiary). (c) **+2 permissions** `academics.enrollment.view`/`.manage` → 337→**339**. (d) **Web** enrollment surface (`/academics/enrollment` — active-model banner + enroll-into-section + a student-subjects resolver view) + `/api/academics/enrollment/*` proxy + nav entry. **Validation:** api/web typecheck green · api lint 0 errors · check:privileged-db green · db:rls:check green (5 new tables) · db:verify **339 / 11 pools** · api unit **582** (+ enrollment 3) · web unit **138** · **e2e 7/7 on real pg** (K-12 section-enroll resolves student→subjects · elective references an offering (non-elective rejected) + joins the set · teacher assigned to an offering · tertiary per-course registration resolves · schoolType fallback · campus-scope deny/allow · RLS isolation + HTTP 401). The e2e caught + fixed a real resolver bug (elective offerings were miscounted as `core`). Isolated `next build`/`nest build` **deferred to CI** (dev servers live); authenticated visual pass owner-gated. **Next:** open PR → review → merge → WB2-2 `done`, which flips **WB2-3** (student lifecycle) `ready`. _(claude)_

- `2026-08-05` — **WB2-1 → `done` (merged); WB2-2 claimed (claude).** [PR #68](https://github.com/Ewosoft-Solutions/claude-trial/pull/68) squash-merged to `main` as `fb671f4`, **CI green** (isolated `next build`/`nest build` + `db:deploy` applying the migration fresh + `db:rls:check` + `check:privileged-db` all passed) — gate met (CI-green **+** merged), so **WB2-1 flips `done`.** With the ADR-02 structure (`ClassSection`/`SubjectOffering`) now on `main`, **WB2-2 is unblocked and claimed (claude)** — branch `feat/wb2-2-enrollment`. Building **enrollment + per-course registration + electives + teacher assignment**, additive over the legacy `Enrollment`(→Class)/`ClassTeacher`: a lightweight effective-dated **`AcademicProfile`** (`enrollmentModel: 'class'|'course'`, defaulting from `Tenant.schoolType` — nursery/primary/secondary→class, university/college/training*institute→course) selects the model; **`SectionEnrollment`** (K-12: Student→`ClassSection`, takes its `SubjectOffering`s as a set), **`CourseRegistration`** (tertiary: Student→`SubjectOffering`), **`StudentSubjectElection`** (electives reference an offering, not free text), **`OfferingTeacher`** (teacher→offering, not a label); a `resolveStudentSubjects` resolver picks class-vs-course by profile. Campus-scoped via the WB1-6 `AccessScopeService`. *(claude)\_

- `2026-08-05` — **WB2-1 built to DoD → `in-review`.** Branch `feat/wb2-1-academic-structure`. The ADR-02 structured academic model that retires the labeled-bag `Class`/`Course` + name-parsing, **additive** over the legacy tables. Delivered: (a) **5 new `academic-structure` tables** — `Stage → YearLevel → ClassSection` (on a `Campus`) ← `Stream`, plus `SubjectOffering` (an F6 `CurriculumSubject` offered to a section in an academic year/term) — all tenant-owned with own+platform **RLS** (`db:rls:check` green on the new tables); `displayLabel` is **COMPOSED** from the dimensions and stored, **never parsed** (grep-guard clean: no `.split`/regex derives stage/year/stream from a label — only `.join(' ')`); migration `20260805030000_academic_structure_model` (additive, idempotent, hand-written FKs incl. `class_sections.campus_id → tenant.campuses`). (b) **`AcademicStructureModelService` + controller** at `/academics/structure/*` (stages/year-levels/streams/sections/offerings CRUD + `campuses/:id` tree read), `TenantDbService`-only (**no new `DatabaseService`** — `check:privileged-db` green), full command path permission→validation→mutation→**audit**→state. **Campus scope ENFORCED** via the WB1-6 `AccessScopeService.assertWithinScope` on section/offering writes (`ClassSection.campusId` is the target) — a Campus-A actor can't build on Campus-B. (c) **+2 permissions** `academics.structure.view` (clr 3) / `.manage` (clr 7) → 335→**337** (`EXPECTED_PERMISSION_COUNTS` bumped; `db:verify` 337/11 pools). (d) **Web** guided **class-builder** (`/academics/structure` — structured campus→year→stream→section picker with a live composed-label preview replacing free-text class names, building-block create forms, sections list, all states, sonner toasts) + a dedicated `/api/academics/structure/*` proxy + nav entry (gated `academics.structure.view`). **Validation:** api typecheck + web tsc green · api lint 0 errors · check:privileged-db green · db:rls:check green · db:verify **337 / 11 pools** · api unit **574** (+4 label-composition) · web unit **138** · **e2e 8/8 on real pg** (SS1 SCIENCE vs SS1 ARTS as two distinct rows without parsing · composed unstreamed label + dedupe · subject offering + dedupe · campus-scope deny/allow · RLS tenant isolation · HTTP 401). The isolated `next build`/`nest build` is **deferred to CI** — `next dev` (:3001) + api dev (:3030) were live locally (the shared-`.next`/`dist` corruption gotcha); authenticated visual pass owner-gated (credential guardrail; WB1-1..1-6 precedent). **Next:** open PR → independent review → merge → WB2-1 `done`, which flips **WB2-2** (enrollment + course-registration) `ready`. _(claude)_

- `2026-08-05` — **WB1-6 → `done` (merged); WB2-1 claimed (claude).** [PR #67](https://github.com/Ewosoft-Solutions/claude-trial/pull/67) squash-merged to `main` as `0d9261d`, **CI green on the merge run** (5m30s) — gate met (CI-green **+** merged), so **WB1-6 flips to `done` → Workbench-1 is 6/6 complete.** Independent review before merge confirmed every surface (maker-checker SoD · step-up · per-route permission guards · campus-scope enforcement via `AccessScopeService` · grant expiry on the live authz path · RLS on `campuses` + the shared-catalog two-policy tightening · audit on every mutation · no privileged client); one nit found + applied — `RequestGrantDto.scope` was a nested object without `@ValidateNested()`/`@Type()`, so class-validator didn't descend into it (the service re-validates scope, so defense-in-depth, not a hole) — committed on the branch before merge. With `Campus` + `AccessScopeService` now on `main`, **WB2-1 is fully unblocked and claimed (claude)** — branch `feat/wb2-1-academic-structure`; building the ADR-02 structured model (Stage · YearLevel · Stream · ClassSection[campus-scoped] + SubjectOffering linking F6 `CurriculumSubject`), `displayLabel` **stored not parsed**, `+2` permissions (`academics.structure.view`/`.manage`), campus scope enforced through the WB1-6 primitive. _(claude)_

- `2026-08-05` — **WB2 kickstarted → detailed; WB2-1 `ready`.** With `F1`+`F6` `done` and ADR-02/03 accepted, Workbench-2 (academic structure + student lifecycle) is now detailed into cards ([`workbench-academic.md`](workbench-academic.md)) and added to the board as **WB2-1..WB2-4**. **WB2-1** (the ADR-02 structured model — campus·stage·year·stream·section + offerings, label stored not parsed) flips to **`ready`**: its hard deps (`F6`, ADR-02/03) are done, and its one new dep — **`Campus`** — lands with WB1-6 (in-review, complete). WB2-2/3/4 stay `backlog` (chained on WB2-1). Note the **cross-workbench link**: WB1-6's `Campus` + `AccessScopeService` are what make the ADR-02 model's campus dimension real — WB2 is where campus scope becomes visible on academic rows, closing Workbench-1 scenario 2 end-to-end. _(claude)_

- `2026-08-05` — **WB1-6 built to DoD → `in-review`** ([PR #67](https://github.com/Ewosoft-Solutions/claude-trial/pull/67), branch `feat/wb1-6-scope-expiry-maker-checker`). Delivered: (a) `UserTenantRole.scope`/`expiresAt`/`grantReason` + the **live** authz path (`getUserPermissionContext`) stops honouring an **expired** grant — a 5-day substitute role auto-expires with no token invalidation (scenario 3); (b) **`AccessGrantService`** routes a **high-risk** grant (a role the WB1-5 `EffectiveAccessService` flags sensitive, or clearance ≥ 7) through a `MakerCheckerRequest` + step-up (`users.role.assign`), applied only when a **second** approver (maker ≠ checker) signs off (scenario 4); low-risk applies directly; approve/reject/revoke; (c) a minimal **`Campus`** model (tenant schema, RLS — ADR-11 Option A) + reusable **`AccessScopeService.assertWithinScope`** that ENFORCES campus scope (WB1-5 only explained it) — proven now on the grant surface, and the primitive WB5/WB2 consume to finish scenario 2; (d) the **shared-catalog RLS tightening** the WB1-5 review flagged — `roles`/`permission_pools`/`role_templates` moved from a single `FOR ALL` policy to the two-policy **shared-read SELECT + own-write** shape (a tenant can no longer DELETE a shared row); (e) web person-detail **Access & scope** panel (grant dialog + active grant + F8 `ApprovalPanel` for pending approvals) + proxies. **+3 permissions** (`access.grants.manage`/`campus.view`/`campus.manage`) → 332→**335**. **Validation:** `ci:quick` typecheck + api lint + web lint + web tsc green · `check:privileged-db` green (no new `DatabaseService` — services use `TenantDbService`) · `db:rls:check` green (campuses covered; catalog retains its PERMISSIVE `tenant_isolation`) · `db:verify` **335 / 11 pools** · api unit **570** (+19: access-scope 10, access-grant 9) · **e2e 7/7 on real pg** (low/high-risk routing · maker≠checker denied + second-approver applies · auto-expiry via live context · campus-scope deny/allow · RLS isolation · 401). Web `next build` deferred to CI (a `next dev` server was live locally — the shared-`.next` corruption gotcha); authenticated visual pass owner-gated (credential guardrail; WB1-1..1-5 precedent). **Next:** open PR → independent review → merge → WB1-6 `done` (**Workbench-1 6/6**), which also flips WB2-1 fully unblocked. _(claude)_

- `2026-08-05` — **WB1-6 claimed (claude), last Workbench-1 slice.** Branch `feat/wb1-6-scope-expiry-maker-checker`. Building **time-boxed + scoped access grants with maker-checker/step-up for high-risk changes**: (a) `UserTenantRole` gains `scope` + `expiresAt` + `grantReason`, and the **live per-request** authz path (`getUserPermissionContext`) stops honouring an **expired** grant — so a 5-day substitute role auto-expires with no token invalidation (scenario 3); (b) a new **`AccessGrantService`** routes a **high-risk** grant (a role the WB1-5 `EffectiveAccessService` flags **sensitive**, e.g. payroll/export, or clearance ≥ 7) through a `MakerCheckerRequest` + **step-up**, applied only when a **second** approver (maker ≠ checker) signs off (scenario 4); low-risk grants apply directly; (c) a minimal **`Campus`** model (tenant schema, RLS — ADR-11 Option A org-within-tenant) is the concrete scope target, and a reusable **`AccessScopeService.assertWithinScope`** ENFORCES campus scope at the access layer (WB1-5 only _explained_ it) — the primitive WB5 finance / WB2 academics consume to complete scenario 2 end-to-end once their rows carry a campus; (d) the shared-catalog RLS tightening the WB1-5 review flagged — `roles`/`permission_pools`/`role_templates` move from a single `FOR ALL` policy (which lets a tenant DELETE a shared row) to the two-policy **shared-read SELECT + own-write** shape (mirrors F6 curriculum). **+3 permissions** (`campus.view`/`campus.manage`/`access.grants.manage`) → 332→**335**. `Campus` is owned here and consumed by WB2 (see WB2 kickstart). _(claude)_

- `2026-08-05` — **WB1-2 + WB1-5 → `done` (merged); WB1-6 → `ready`.** [PR #63](https://github.com/Ewosoft-Solutions/claude-trial/pull/63) squash-merged to `main` as `7c69db5`, **CI green on the `main` merge commit** — gate met (CI-green **+** merged), so both slices flip to `done`. Independent maker-checker review returned **APPROVE-WITH-NITS** with all findings applied (notably: the new role effective-access reads — `effective-access`/`explain`/`affected`/`templates`/`preview` — were only auth+tenant-guarded because `ClearanceLevelGuard` was absent from the controller chain, making the `@RequireClearanceLevel` decorators inert; fixed by adding the guard + gating those reads at clearance 7). Final validation: `ci:quick` + `check:privileged-db` + `db:rls:check` green, `db:verify` **332 perms / 11 pools** intact, api unit **545**, web unit **138**, e2e **15/15** on real pg (staff-employment 8 + role-editor 7). With WB1-5 `done`, **WB1-6 flips `backlog` → `ready`** (dep met) — it carries scope/expiry + maker-checker for high-risk grants, and now also absorbs the deferred campus-scope **enforcement** and the shared-catalog (`role_templates`/`roles`/`permission_pools`) RLS tightening the review flagged as a repo-wide follow-up. Workbench-1 is now **5/6 done** (only WB1-6 remains). _(claude)_

- `2026-08-04` — **WB1-2 + WB1-5 built to DoD → in-review** ([PR #63](https://github.com/Ewosoft-Solutions/claude-trial/pull/63), one combined branch `feat/wb1-2-5-staff-employment-role-editor`, owner-chosen; independent maker-checker review **APPROVE-WITH-NITS**, all findings applied). **WB1-2 — first-class staff employment:** extended the F1 `StaffProfile` into a MANAGED employment domain (reporting-line self-relation + loose `UserTenant` link + `endReason`/`updatedBy` + migration source keys) + a new `person.staff_qualifications` child table (RLS); a `StaffEmploymentService` + controller under `directory/people/:personId/employment` (list/create/**disable independent of any payroll run**/update/reporting-line with self+cycle guard/qualifications/managers-picker — gated `staff.view`/`staff.create`/`staff.edit`/`staff.delete`, **no new permission**); an additive migration (`20260805000000`) with a `set_config` DO-block **payroll→employment back-fill** keyed on `(tenant, source_system, source_id)`; a person-detail **Staff employment** panel (create/edit/disable/qualifications). The People-directory Staff view already reads `StaffProfile` (WB1-1), so the "reads Employment not payroll" acceptance holds. **WB1-5 — role editor + effective-access evaluator:** a new `roles-permissions.role_templates` preset table (nullable-tenant shared-read RLS, mirroring `roles`) + 5 seeded system templates (Bursar/Registrar/Teacher/Form teacher/IT support); additive `roles.scope` + `roles.template_key`; an **`EffectiveAccessService`** (per-permission matrix with source pool + plain-language reason, sensitive-action surfacing, separation-of-duties conflict detection, scope-aware `explain()`, who's-affected) + `RoleTemplateService` (name→pool-id resolution); new endpoints (`GET /roles/templates`, `POST /roles/preview`, `GET /roles/:id/effective-access`, `POST /roles/:id/explain`, `GET /roles/:id/affected`) with `createCustomRole` extended to persist scope+templateKey; permission search reuses the existing `GET /permissions`. Web: the previously-dead `settings/roles` **"Add role"** is now a working editor (template → scope → **live effective-access preview** with permission search + destructive **sensitive** badges + a `--warning` SoD callout) and any role opens an effective-access + who's-affected explainer. **Campus-scope ENFORCEMENT + expiry + maker-checker stay WB1-6** (its declared dep); WB1-5 delivers the editor + evaluator + preview, and the evaluator EXPLAINS scope (a Campus-A role denies a Campus-B probe). **Validation:** `ci:quick` (build+lint+typecheck) green · `check:privileged-db` green (no new `DatabaseService` injection — new services use `TenantDbService` or take a scoped prisma) · `db:rls:check` green (both new tables covered) · `db:verify` **332 permissions / 11 pools** intact (templates aren't permissions) · api unit **544/544** (+8: employment 4, effective-access 4) · web unit **138/138** · **e2e 15/15 on real pg** (`staff-employment` 8 + `role-editor` 7). Authenticated visual pass owner-gated (credential guardrail; WB1-1/WB1-3/4 precedent) — the web build is validated by `next build` in `ci:quick`. **Next:** review → merge → both `done`; then **WB1-6** (scope enforcement + expiry + maker-checker) unblocks. _(claude)_

- `2026-08-04` — **WB1-2 + WB1-5 claimed (claude), one combined branch** (`feat/wb1-2-5-staff-employment-role-editor`; owner chose one combined PR, mirroring the WB1-3/4 precedent). **WB1-2** — make the F1 `StaffProfile` a first-class managed **employment** domain: extend it with a reporting-line self-relation + `UserTenant` account link + migration source keys, add a `StaffQualification` child table (RLS), a `StaffEmploymentService` + controller under `directory/people/:personId/employment` (create/update/**disable independent of any payroll run**; reporting line; qualifications — gated by the existing `staff.view`/`staff.create`/`staff.edit`/`staff.delete`, no new permission), a payroll→employment backfill with source keys, and a person-detail **Staff employment** panel; the People directory Staff view already reads `StaffProfile` (WB1-1), enriched here. **WB1-5** — the permission-management UX the 332-permission engine has been missing: a new `RoleTemplate` preset domain (nullable-tenant shared-read RLS) + an **effective-access evaluator** (per-permission decision + human explanation: source pool/template/override, clearance, scope, SoD conflicts, sensitive-action surfacing), permission search over `resource.action.context`, who's-affected, and a working **role editor** wiring the currently-dead `settings/roles` "Add role" over the F8 Policy shell. Full campus-scope enforcement + maker-checker stays **WB1-6** (its declared dep); WB1-5 delivers the editor + evaluator + preview. _(claude)_

- `2026-08-04` — **WB1-2 + WB1-5 → `ready`; next batch queued for a future session.** With WB1-1/WB1-3/WB1-4 `done`, the next Workbench-1 slices with all deps met flip `backlog` → `ready`: **WB1-2** (first-class staff employment/profile — retire payroll-as-directory; dep F1 ✓) and **WB1-5** (role editor + `resource.action.context` matrix + effective-access preview; deps F1 + ADR-01 ✓). Owner will **start these in a separate session** (not claimed yet — the next session sets Owner + `claimed` per `WORKFLOW.md` before coding). **WB1-6** (scope/expiry + temporary cover + maker-checker/step-up) stays `backlog`: its declared dep is **WB1-5**, so it can only start once WB1-5 lands — it follows WB1-5, not parallel to it. (WB1-6 also finally absorbs the deferred **campus-scope** acceptance from WB1-1 and the maker-checker/step-up deferred from WB1-3/4.) _(claude)_

- `2026-08-04` — **H4 added — input-validation hardening (proposed).** A caregiver search accepting `??` surfaced a systemic gap: ~45 web input surfaces with almost no client validation, and ~230 unbounded/unpatterned server strings (439 `@IsString` vs 206 `@MaxLength`, 5 `@Matches`). Plan of record: [`docs/input-validation-plan.md`](../../../docs/input-validation-plan.md) — validate by field _type_ (name/email/phone/amount/…), client (instant feedback) + server (authoritative), one canonical rule table, shared web `lib/input-validation.ts` module (seeded in PR #60) + composed API decorators, phased rollout (identity/security → records → money/sensitive → academics/ops) + a CI guard against new unbounded strings. _(claude)_

- `2026-08-04` — **WB1-3 + WB1-4 → done (merged).** [PR #60](https://github.com/Ewosoft-Solutions/claude-trial/pull/60) squash-merged to `main` as `97ac560`; **CI green on the `main` merge commit** — gate met (CI-green **+** merged), so both slices flip to `done`. Independent maker-checker review returned **APPROVE-WITH-NITS** — every WB1-3/WB1-4 acceptance item verified against code + passing tests, full validation green (api unit **536**, web **138**, **e2e 11/11** on real pg, `ci:quick` + `check:privileged-db` + `db:rls:check` green, permissions **332**). All findings applied: **(moderate)** a DB backstop for the exactly-one-primary invariant — partial unique index `guardian_relationships_one_primary_per_ward` + demote-before-promote ordering + a clean 409 on the rare concurrent-promotion race (migration `20260804140000`, e2e-proven); **(nits)** dropped 3 unused audit constants, corrected the SecureLink/reactivate docstrings (incl. a SecureLink caller-supplied-token usage note), and added e2e for admin-reset — both token issuance **and** a full redeem-through. Both slices are **done** (merged, CI-green on `main`). Also this session: extensive UX refinement on the WB1-3/4 surfaces (search fixes, input validation + [H4 plan #61], design polish, table UX = mobile scroll + default-10 + clean `sort=-name` + cookie **and per-account** page-size preference, direction-aware caregiver labels, `caregiver` relationship type) and a **check-in/out attendance** initiative flagged for a future session. _(claude)_

- `2026-08-04` — **Future initiative flagged: check-in/out attendance (deferred).** Owner asked to design "soon", in a **later session**: scan-based **check-in/out** for **students, staff, and caregivers** (e.g. **househelps** who collect kids — registered as a `Person`, photo captured, scan-in to pick up + scan-out). Groundwork landed in WB1-4 ([PR #60](https://github.com/Ewosoft-Solutions/claude-trial/pull/60)): a first-class **`caregiver`** relationship + existing `canPickup`/`isEmergencyContact`/consent on `GuardianRelationship`. Not started; design as its own initiative (a `docs/attendance-plan.md`) near **WB8** (daily-work) / **WB9** (operational depth) — shared check-in-event domain (subject = student|staff|caregiver), scan credential (QR/badge/biometric; note deferred per-staff-QR #12 + WebAuthn), a gate/pickup verification screen (photo + authorised collectors via `resolveAudience`/`canPickup`), F4 photo capture, F3 jobs + audit ledger. _(claude)_

- `2026-08-04` — **WB1-3 + WB1-4 built to DoD → in-review.** Both slices on `feat/wb1-3-4-provisioning-guardianship` (one combined PR, owner-chosen). **WB1-3** — new `provisioning` module: `AccountProvisioningService` + controller under `/directory/people/:id/account` (`GET` state · `POST` invite/resend-invite/suspend/reactivate/reset-password, all gated **`users.provision`**). Invite mints an expiring **F5 `SecureLink`** (hashed at rest, `maxUses:1`), stores the same raw token on `UserTenant.invitationToken` so the **existing accept-invite page** still resolves it, and delivers via **F5 `DeliveryService`** (`critical`, ledgered) — the legacy `INVITATION_EMAIL_JOB` is suppressed (`UserInvitationService.createInvitation` gained `{skipLegacyEmail, invitationToken}`). Accept → the user sets their **own** password (no plaintext ever emitted; regression-proven). Suspend sets `status=suspended`+`suspended=true` (the exact flags `authentication.service` refuses login on) + revokes sessions + audits; reactivate restores. Admin reset reuses `User.passwordResetToken` (via new `issueAdminPasswordReset`) + a `password_reset` SecureLink + F5 delivery. **User-row writes stay on the grandfathered privileged client** (no new `DatabaseService` injection — `check:privileged-db` green). **WB1-4** — extended F1 `GuardianRelationship` (the Person→Person model backing the Guardians tab) with **custody, canPickup/canAuthorizeMedical/isEmergencyContact/isBillingContact, per-category consent (results/finance/attendance/general), verification** (additive migration `20260804000000`, RLS unchanged — already covered); new `GuardianshipService` + `/guardianships` controller (list/create/update/verify/end + **`resolveAudience(ward, category)`** = comms recipients by relationship+consent, emergency ignores consent — gated `guardians.view`/`guardians.manage`); legacy `StudentGuardian` marked deprecated read-only. **Full workbench UI**: account panel + guardianship panel on the person detail (invite/suspend/reactivate/reset; add/edit/verify/end guardians with authority+consent), all states, `sonner` toasts. **+2 permissions** (`users.provision` + `guardians.manage`) → **332** (`EXPECTED_PERMISSION_COUNTS` updated; seed verifies 332). **Validation:** `ci:quick` green · `check:privileged-db` green · `db:rls:check` green · api unit **531/531** (+17 new: guardianship 11, provisioning 6 incl. the no-plaintext guard) · web unit **120/120** · **e2e 8/8 on real pg (app_runtime)** — invite→SecureLink+DeliveryAttempt+no-password, accept→own-password→active, suspend flags, two-guardians authority/priority, audience-by-consent (finance excludes opt-out, emergency ignores consent), verify+end, RLS isolation, HTTP 401 boundary · Prettier-clean on all touched files. Authenticated visual pass owner-gated (credential guardrail; WB1-1 precedent). **Next:** review → merge → WB1-3 + WB1-4 `done`. _(claude)_

- `2026-08-04` — **WB1-3 + WB1-4 claimed (claude), one combined branch.** Building **secure provisioning** (WB1-3: invitation → user sets own password via an expiring **F5 `SecureLink`** + delivery via **F5 `DeliveryService`**; activation/suspension that **blocks login** + audits; admin-initiated reset; **no code path emits a plaintext password** + regression guard) and **guardianship authority/priority/consent** (WB1-4: extend F1 `GuardianRelationship` — the Person→Person model that already backs the Guardians tab — with **custody, per-category contact-consent, verification, effective range**; multiple non-parent caregivers; a **consent-aware audience resolver** so results/fee comms target by relationship+consent, never a gender label; legacy `StudentGuardian` deprecated read-only) on `feat/wb1-3-4-provisioning-guardianship`. **Full workbench UI** on the person detail (owner-chosen). **+2 permissions** planned: `users.provision` (SYSTEM*ADMIN, clearance 7) + `guardians.manage` (GUARDIAN, clearance 5) → 330→**332** (`EXPECTED_PERMISSION_COUNTS` updated same commit). *(claude)\_

- `2026-08-04` — **WB1-1 → done (merged to `main`).** The Unified People directory shipped across **[PR #53](https://github.com/Ewosoft-Solutions/claude-trial/pull/53)** (`a8eea44`, directory + tabs) + **[#54](https://github.com/Ewosoft-Solutions/claude-trial/pull/54)** (`08395d0`, cards-as-selector, per-table filters, phone, detail drawer + profile) + **[#56](https://github.com/Ewosoft-Solutions/claude-trial/pull/56)** (`c0639ce`, **Person 360** — tabbed detail drawer + profile + cross-domain roll-ups). An independent completeness review verdict is **COMPLETE-WITH-GAPS** (merged code: unit **58/58**, e2e **7/7**, `check:privileged-db` + `db:rls:check` green). Delivered: five/six person-type tabs incl. **All** roster + summary cards; **one identity, many profiles**; masked contact via `people.view_contact`; per-tab permission enforced server-side; **person 360 detail route** (relationship timeline + academics/finance/documents/people domain tabs, each profile/permission-gated); audited masking-aware CSV export; **+1 `guardians.view`** (catalog **330**). **Deferred gaps (not blockers):** campus scope (acceptance "scoped admin sees only their campus" — no `Campus` model yet → **WB1-6**); HTTP guard-stack RBAC-denial e2e (tracked as **F2-fu1**); minor DTO doc drift (`type` default). This is the **first Workbench-1 slice done**; **WB1-2…WB1-6 remain `backlog`** (Workbench-1 ≈ 1/6). _(claude)_

- `2026-08-03` — **WB1-1 enhancements (owner feedback), still in-review.** Added an **"All" roster tab** (first + default; every person, gated on `people.view`; role chips + masked contact), **summary cards** (`GET /directory/people/summary` → per-tab counts for only the tabs the caller may view, rendered as a clickable F8 `StatGrid`), and a guarded **dev staff seed** (`db:seed:people`) so every tab shows data (8 staff incl. 2 contractors, 32 contact points, 4 staff-and-guardian identities). No new permissions (All reuses `people.view`; catalog stays **330**). api unit **41/41**, **e2e 7/7**, `ci:quick`+`check:privileged-db`+Prettier green. Clarified: _prospect = prospective student (admission applicant)_; _vendors/contractors/external auditors_ = `StaffProfile` `employmentType:'contract'` (engaged) or a scoped `User` (limited login) today; a dedicated external-party type is a deferred WB1-5/6 decision. _(claude)_

- `2026-08-03` — **WB1-1 built to DoD → in-review.** The unified **People directory** — the first Workbench-1 slice (includes web UI) — shipped on `feat/WB1-1-people-directory`. **One `WorkbenchLayout` (F8)** with five person-type tabs — **Students · Guardians · Staff · Users · Prospects** — each rendering the F7 `DirectoryTable` over a governed **Person-centric projection**: student/guardian/staff/user tabs project the F1 `Person` anchor (so **one identity lists every profile it holds** — the acceptance), Prospects project `AdmissionApplication`. **API:** new `PeopleDirectoryService` (per-tab projection, masked contact via `person.masking` unless `people.view_contact`, explicit `select` that never touches health/safeguarding, `TenantDbService.client` only) + `PeopleDirectoryController` (base gate `people.view`; per-tab type permission enforced server-side: `students.view`/`guardians.view`/`staff.view`/`users.view`/`admissions.view`; audited per-tab CSV export). **+1 permission** `guardians.view` (329→**330**; `EXPECTED_PERMISSION_COUNTS` updated same commit; seed verified 330, guardians.view → 8 pools). Saved-views broadened to `people-<type>` resources (ANY of `students.view`/`people.view`). **Web:** `/people` workbench (server-side page/filter/sort + SavedView + shareable URL + masked contact + per-tab bulk export + permission-denied state for tabs you lack); new nav section. **Validation:** `ci:quick` (build+lint+typecheck) · `check:privileged-db` · `db:rls:check` · Prettier-clean · api unit **37/37** (service masking/projection/injection-guard + controller unauthorized-tab) · **e2e 6/6 on real pg (app_runtime)** — masking with/without contact scope, tenant isolation across all tabs, per-tab projection, one-identity-two-profiles, audited export · HTTP `GET /directory/people` returns **401** unauth (guard stack) · `/people` 307→`/login` unauth. Authenticated visual pass owner-gated (credential guardrail; F7 precedent). **Next:** review → merge → WB1-1 `done`. _(claude)_

- `2026-08-03` — **WB1-1 claimed (claude).** Building the **Unified People directory** to DoD on `feat/WB1-1-people-directory` — the first Workbench-1 feature slice (so it includes web UI, unlike the F-foundations). One `WorkbenchLayout` (F8) with person-type tabs (Students · Guardians · Staff · Users · Prospects), each rendering the F7 `DirectoryTable` over a governed **Person-centric projection** (`person` schema) — student/guardian/staff/user tabs project over `Person` (so one identity shows all its profiles — the acceptance), Prospects project over `AdmissionApplication`. Reuses the F7 `directory` module pattern: masked contact via `person.masking` unless the caller holds `people.view_contact`, explicit `select` that never touches health/safeguarding, `TenantDbService.client` only. Base gate `people.view` (F1); per-tab type permission (`students.view`/`staff.view`/`users.view`/`admissions.view` reused; **+1 new `guardians.view`** — genuinely new capability). e2e on real pg (masking · tenant isolation · per-tab projection · unauthorized scope). _(claude)_

- `2026-08-02` — **F8 → done (merged to `main`).** [PR #52](https://github.com/Ewosoft-Solutions/claude-trial/pull/52) squash-merged after an independent maker-checker review (3 a11y findings fixed: `PolicyVersionPanel` `listbox`→`aria-pressed` button group; `LifecycleBar` per-step `sr-only` status; `ApprovalPanel` `sr-only` before→after) + a two-step board/seed reconcile against F5+F6. The four shared Aurora workspace shells (`WorkbenchLayout`/`LifecycleBar`/`PolicyVersionPanel`/`ApprovalPanel`) now on `main`. **This completes the Phase-1 foundations — F1–F8 all `done`.** **WB1 (People directory)** is fully unblocked (compose `WorkbenchLayout` + F7 `DirectoryTable`); `F9` is the only remaining Phase-1 item (now `ready`). _(claude)_
- `2026-08-02` — **F9 → ready.** Its deps F3 (jobs/outbox) + F4 (documents) are done/merged, so the export/retention/privacy primitives (`DataExportJob`, retention) are now claimable. Last remaining Phase-1 foundation. _(claude)_
- `2026-08-02` — **F8 built to DoD → in-review.** The four remaining shared Aurora workspace shells shipped in `packages/ui` on `feat/F8-aurora-patterns` (design bridge 08), beyond F7's Directory: **`WorkbenchLayout`** (persistent context-bar the workspace inherits + a controlled tab strip — fixes the legacy re-pick-context-per-page C044+), **`LifecycleBar`** (a record's status view; current state carries a tone AND a non-colour cue — ring + `aria-current` — completed states a check), **`PolicyVersionPanel`** (versioned config: a version rail + **clone / compare / activate** + a before/after diff — the shape F6 curriculum versions + WB1-5 role policy reuse), **`ApprovalPanel`** (the maker-checker surface: before→after change, a **separation-of-duties block** that disables approve when reviewer == requester, a step-up notice). All presentational + controlled + data-driven, styled purely with Aurora semantic tokens (no hardcoded colour → light/dark/classic-dark parity by construction) + WCAG focus/contrast. New `types/patterns.types.ts`. **Validation:** vitest **14/14** on the four shells (render + interaction + a11y), full `packages/ui` suite **161** green; **`ci:quick` green** (build+lint+typecheck; fixed a `SelectTrigger size` typing slip in the demo); **visual pass across all 3 themes** on `/design-system/patterns` (dev :3001) — the demo renders **two workspaces (People + Academics) from the same shell** (acceptance) + all four shells; screenshots captured in dark, light, classic-dark. `/design-system` is auth-gated, so the preview was made public **locally only and reverted** (never committed) — no credential entered (guardrail). No DB/permissions. **This completes Phase-1 foundations** (F1–F8 built). **WB1 (People directory)** is now fully unblocked (F1 ✓ + F7 ✓ + **F8** ✓) and can compose `WorkbenchLayout` + `DirectoryTable`. **Next:** review → merge. _(claude)_
- `2026-08-02` — **F8 claimed (claude).** Building the shared Aurora workspace patterns in `packages/ui` on `feat/F8-aurora-patterns` (design bridge 08): the four remaining reusable shells beyond F7's Directory — **Workbench** (persistent context-bar + tabs), **Lifecycle** (status/state views via `StatusBadge` tones, non-colour cue), **Policy** (versioned config with clone/compare/activate), **Approval** (maker-checker surface with separation-of-duties guard + step-up). Presentational + controlled + Aurora-token styled (light/dark/classic-dark parity, WCAG focus/contrast); vitest component tests; a `/design-system/patterns` demo rendering **two workspaces from the same shells** (acceptance). No new permissions/DB — pure UI. Independent of F5/F6 (branched off `main`). _(claude)_

- `2026-08-02` — **F6 → done (merged to `main`).** [PR #49](https://github.com/Ewosoft-Solutions/claude-trial/pull/49) squash-merged after an independent maker-checker review (5 findings fixed: **RLS backstop** — per-command reference policies so national rows aren't writable/deletable by tenants; published-version immutability; provenance gate no longer self-attestable; adoption supersede; dead-param cleanup). New `curriculum` schema on `main` (Authority→…→LearningOutcome + Adoption + Overlay + Mapping), **+4 permissions (325→329)**. e2e 8/8. **Unblocks WB2** (subject catalog) + **WB4** (results reference a version) + **WB8** (coverage). **Phase-1 foundations: F1–F7 all done; only F8 (#52) in-review.** _(claude)_

- `2026-08-02` — **F6 built to DoD → in-review.** The academic-profile + policy-version framework shipped → [PR #49](https://github.com/Ewosoft-Solutions/claude-trial/pull/49) (ADR-03). **DB (new `curriculum` schema, hand-written migration `20260802010000`):** `CurriculumAuthority`→`CurriculumFramework`→**`CurriculumVersion`** (effective-dated + approval-state + provenance + `is_national_immutable`)→`CurriculumStage`→`CurriculumSubject`→`CurriculumNode`(strand/topic)→`LearningOutcome`, plus `CurriculumAdoption` (cohort-effective), `TenantCurriculumOverlay`, `CurriculumMapping` (aliases). **Tenanting:** the 7 reference tables carry a **nullable** `tenant_id` (NULL = shared national content) with a **two-shape RLS**: reference tables READ own+national(NULL)+platform but WRITE own+platform (so national content is immutable to tenants at the DB layer); the 3 application tables use standard isolation. Added `curriculum` to `schema.prisma` + `rls-coverage-check.sql`; `db:rls:check` green (all 10 tables). **API `CurriculumModule`:** `CurriculumService` (authoring + `assertWritableVersion` immutability guard + **activation provenance gate** — refuses to publish a version with unreviewed AI/imported nodes, fixes C081), `CurriculumAdoptionService` (adopt + **effective-dated `resolveForCohort`**), `CurriculumOverlayService`, `CurriculumMappingService` (normalized-name alias de-dup, fixes C080). 3 guarded `@TenantScoped` controllers. **Permissions +4** (`curriculum.view`/`manage`/`activate`/`adopt`; new `CURRICULUM_PERMISSIONS` array; total on this branch 320→324 — reconciles to 329 with F5's +5 at merge). **Validation (real pg, app_runtime):** curriculum e2e **5/5** (two cohorts→different versions in one campus + prior version untouched on new activation · AI-node activation gate · national readable-but-immutable + overlay path · RLS isolation · dirty-name alias resolution); api unit **470/470** (+ `normalizeName` unit); `ci:quick` + `check:privileged-db` + `db:rls:check` + seed catalog (324) green; Prettier-clean. **Next:** review → merge → F6 `done` (unblocks WB2 subject catalog + WB4 results-reference-a-version + WB8 coverage). Optional follow-ups: seed the official NERDC framework versions as production reference data; wire lesson/offering→LearningOutcome citation (WB8); resolve overlays into the read-time tree. _(claude)_
- `2026-08-02` — **F6 claimed (claude).** Building the **academic-profile + policy-version framework** to DoD on `feat/F6-academic-profile` (ADR-03): a new `curriculum` schema — `CurriculumAuthority` (NERDC/Cambridge/tenant) → `CurriculumFramework` → **`CurriculumVersion`** (effective-dated + approval state + provenance + national-immutable flag) → `CurriculumStage` → `CurriculumSubject` → `CurriculumNode` (strand/topic) → `LearningOutcome`, plus **`CurriculumAdoption`** (cohort-effective: Primary-1/JSS-1 can run different versions in one campus), **`TenantCurriculumOverlay`** (tenant edits layered over an immutable national version, never mutating the source), and **`CurriculumMapping`** (old↔canonical subject aliases for transfer/migration de-dup). National content immutable once imported; AI/imported nodes carry provenance + a named reviewer (fixes C081); an academic owner gates version activation. Server-side enforcement + RLS on every new table + effective-dated resolution + a seeded official NERDC version + e2e on real pg. _(Note: independent of F5 — branched off `main`; F5 is in-review as PR #48. Expect a board merge reconcile.)_ _(claude)_
- `2026-08-02` — **F5 → done (merged to `main`).** [PR #48](https://github.com/Ewosoft-Solutions/claude-trial/pull/48) squash-merged after an independent maker-checker review (6 findings fixed: category-aware **marketing opt-in** consent; dead-job **failure recorded on the ledger**; **atomic** SecureLink single-use; **audited** denied redemptions; email-channel template HTML-escaping; adapter-idempotency doc). Communication delivery layer now on `main`: `DeliveryAttempt` ledger + `ContactPreference` + `SecureLink` + templates/campaigns on the F3 job substrate; **+5 permissions (320→325)**. e2e 10/10. **Unblocks WB1-3** (secure invitations) + every notification path. **Phase-1 foundations: F1–F5 + F7 done; F6 + F8 in-review** (PRs #49/#52 — reviewed + fixed + CI-green, pending merge). _(claude)_

- `2026-08-02` — **F5 built to DoD → in-review.** Communication **delivery abstraction** shipped on `feat/F5-communication-delivery` (ADR-07). **DB (`communication` schema, hand-written RLS migration):** `ContactPreference` (consent/DND/quiet-hours — the model `ContactPoint` deferred to), `MessageTemplate`+`TemplateVersion`, `Campaign`+`CampaignRecipient`, the **`DeliveryAttempt` ledger** (channel/provider/status/failureClass/**costUnits+dndFlag**/redactedDestination/attemptNo, idempotent on `(tenant, dedupe_key)`), and `SecureLink` (sha256-hashed token, permission-checked + expiring). All 7 tables ENABLE+FORCE RLS + PERMISSIVE `tenant_isolation` + `app_runtime` grants (`db:rls:check` green). **API (`DeliveryService`):** resolves audience→consent→cost, writes the ledger, enqueues the send on **F3 jobs** keyed to the attempt; `DeliveryJobRegistrar` performs the provider send via a `ChannelAdapter` registry (email delegates to the existing `EmailService`; log SMS/push + in-app adapters); `SecureLinkService` (create/redeem/revoke, per-link permission via `PermissionService` + audience binding), `ContactPreferenceService`, `TemplateService`, `CampaignService` (fan-out through `DeliveryService` so consent applies), `DeliveryLedgerService` (SMS-balance/usage from the ledger). 5 controllers, all guard+`@TenantScoped`. **Permissions +5** (320→**325**): `communication.delivery.view|manage`, `communication.campaigns.manage`, `communication.templates.manage`, `communication.preferences.manage` (seed catalog validated, 5 created + auto-assigned to pools). **No new prod secret** — SecureLink uses a random token + stored hash (avoids the DOCUMENT*URL_SIGNING_SECRET-style guard). **Validation (real pg, app_runtime):** delivery e2e **6/6** (cost+DND reproduces usage · idempotent dedupe · **provider-timeout retry ⇒ no double-send** · SecureLink expiry+permission+audience · campaign consent-suppress + critical-override · RLS isolation); api unit **472/472**; `sms-cost` unit 5/5; `ci:quick` + `check:privileged-db` + `db:rls:check` green; Prettier-clean on touched files. Sends run on F3 with an idempotency key; audience resolves from `GuardianRelationship`+consent, never a gender label; SecureLink fixes C108. **Next:** review → merge → F5 `done` (unblocks WB1-3 invitations + every notification path); then **F6**. *(claude)\_
- `2026-08-02` — **F5 claimed (claude).** Building the communication **delivery abstraction** to DoD on `feat/F5-communication-delivery` (ADR-07): a new `communication`-schema delivery layer — `DeliveryPort` interface + pluggable channel adapters (SMS/email/push, log/no-op adapters for dev+test), a `DeliveryAttempt` ledger (channel/provider/status/failureClass/**costUnits+DND classification**/redactedDestination/attemptNo) that reproduces the legacy SMS-balance + delivery log, `MessageTemplate`+`TemplateVersion` (locale/channel), `ContactPreference` (channel opt-in/consent/quiet-hours; the model `ContactPoint` was left to reference), `Campaign`/`CampaignRecipient` (bulk), and `SecureLink` (permission-checked, **expiring** tokens that replace public result/payment URLs — fixes C108). Sends run on **F3 jobs** with an idempotency key (provider timeout + retry ≠ double-send); audience resolves from real `GuardianRelationship` + consent, never a gender label. Server-side enforcement + RLS on every new table (`db:rls:check`) + e2e on real pg. Next: build → in-review → merge → F5 `done` (then F6). _(claude)_
- `2026-08-01` — **F7 → done (merged to `main`).** [PR #44](https://github.com/Ewosoft-Solutions/claude-trial/pull/44) merged (merge commit) — the governed directory pattern (`DirectoryTable` + `useDirectoryState` in `packages/ui`, tenant-owned `SavedView` + RLS, tenant+permission-filtered students projection with masked contact, audited bulk CSV export). Independent second-agent review APPROVED (3 masking bypasses found + fixed + regression-tested); CI green (api 467 unit + directory e2e + `db:rls:check` + `check:privileged-db`). Phase-1 foundations now `done`: `F1`/`F2`/`F3`/`F4`/`F7`. **WB1 (People directory) now needs only F8** and can reuse this `DirectoryTable`/`useDirectoryState` directly. _(claude)_
- `2026-08-01` — **ADR-14 drafted (Proposed) + WB11 added.** [ADR-14](adr/ADR-14-platform-surfaces-over-tenants.md) — "platform-level surfaces over tenants" — captures three related, owner+DPO-gated sub-decisions surfaced with the owner: **(14A)** cross-tenant identity (use `User.id` for account-holders now; a pseudonymous **platform participant id** when the first cross-tenant feature ships; opt-in `Person`-link only later — `Person` stays tenant-scoped, no ADR-01/11 change); **(14B)** consented cross-tenant **data sharing** for inter-school activities (SSO/OIDC-style minimized, consented **claims release** + `DisclosureLog` + retention; guardian-driven for minors; health as attestation not narrative); **(14C)** **targeted platform notifications** (platform-scoped broadcast, audience **by permission not role-name**, delivered via F5 on F3 jobs with an idempotent `DeliveryAttempt` ledger + a new device/push-subscription registry + VAPID fan-out). New **WB11-\*** (platform-level surfaces) added to later workbenches — blocked on **ADR-14 + F5 + F9 + web-push backend**. No code; docs/decision only. _(claude)_
- `2026-08-01` — **F7 built to DoD → in-review.** Governed directory pattern shipped on `feat/F7-directory-search` (PR open). **`packages/ui`:** pure `directory-state` (URL⇄state), `useDirectoryState` hook, a generic server-driven `DirectoryTable` (selection + **bulk-action bar**, sortable `aria-sort` headers, column-visibility, integrated `custom/states`, `MaskedValue`). **DB:** new `directory` schema + tenant-owned `SavedView` (hand-written RLS migration: ENABLE+FORCE + PERMISSIVE `tenant_isolation` + `app_runtime` grants + tenant-FK cascade; added to `rls-coverage-check.sql`). **API:** `DirectoryModule` — a tenant+permission-filtered students projection (contact **masked** without `students.view.personal_info`; explicit `select` that **never** touches `healthInfo`/medical/safeguarding narrative; record-level class-ownership scope; fees aggregate), `SavedViewService` (owner-scoped + tenant-shared), and a governed **bulk CSV export** (gated `students.export`, audited, masking-aware). **Web:** Students list rewired to the pattern — server-side page/filter/sort, **saved view + shareable URL + bulk export + masked contact**. **No new permissions** (reuses `students.view` / `students.view.personal_info` / `students.export`). **Validation:** api unit 464/464 · ui 147 · web 120 · directory e2e 4/4 (real pg, `app_runtime`: masking + RLS isolation + saved-view owner-scoping + export); `ci:quick` + `check:privileged-db` + `db:rls:check` green; Prettier-clean on touched files. **Next:** review → merge → F7 `done` (WB1 then needs only F8). _(claude)_
- `2026-08-01` — **P0-3 COMPLETE — all 5 owner-gated ADRs signed off → `P0-3` done (12/12).** Owner attended ADR-03/04/05/10/11 in one pass. **ADR-11 → Option A** (one tenant per operating school; campuses = orgs within; owner / campus-manager / cross-campus reporting via scoped memberships — confirms ADR-01's tenant-scoped `Person`). **ADR-03 → Option 1** (versioned curriculum + tenant overlays + cohort-effective adoption; owner deferred to eng, confirmed curricula version over time). **ADR-04 → Option 1** (immutable publication snapshot + amendment; finance never silently blocks). **ADR-05 → Option 1 with amendments** (payer/family account + allocation, **not a custodial wallet** — gateways settle to the school's account, accountants record off-app payments, "credit" = an AR balance; **checkout/cart** billing; **posts double-entry into the GL**). **ADR-10 → build internal auditor-grade GL _and_ integrate** ("no tool → use ours; has tool → integrate") — **supersedes the drafted Option-C defer**; the app is positioned as an auditing platform recording every financial transaction (ops/fees/payroll). Eng sequencing: double-entry backbone in Release-1, statement/close/payroll surface as a fast-follow (**WB5 expected to split**). **Unblocks:** `F6` → `ready`; WB2/WB4/WB5 + multi-campus cleared at the ADR layer (still gated on `F`-deps). Owner also asked for **verifiable/blockchain anchoring** of results + receipts → drafted **[ADR-13](adr/ADR-13-verifiable-anchored-records.md)** (hash-anchoring only, never PII; future/deferred). _(claude)_
- `2026-08-01` — **F7 claimed (claude).** Building the governed search + saved-views + URL-state directory pattern (`#105`/`#27`) to DoD on `feat/F7-directory-search`: a reusable Aurora `DirectoryTable` + `useDirectoryState` hook in `packages/ui`, a tenant-owned `SavedView` table (RLS), a governed tenant+permission-filtered students projection (masked contact without `students.view.personal_info`, never indexes health/safeguarding narrative), and a governed bulk action — the Students list is the first consumer (saved view + shareable URL + bulk action). No new permissions (reuses `students.view` / `students.view.personal_info` / `students.export`). _(claude)_
- `2026-08-01` — **F1 + F4 + F2 → done (merged to `main`).** [PR #42](https://github.com/Ewosoft-Solutions/claude-trial/pull/42) (Person + Documents + Imports foundations, review fixes) and [PR #43](https://github.com/Ewosoft-Solutions/claude-trial/pull/43) (changed-files format gate + editor config) both merged; branches deleted. Phase-1 foundations now on `main`: `F1`/`F2`/`F3`/`F4` done. **Unblocks WB1 (People directory** — F1+F7+F8) and the rest of Workbench-1. Remaining Phase-1 `ready`: `F5`/`F7`/`F8`; follow-ups `F2-fu1/2/3` backlog. _(claude)_
- `2026-08-01` — **PR #42 second-agent review → fixes landed; 3 follow-ups tracked.** Independent review (maker-checker) returned CHANGES-REQUESTED with 2 blocking: (1) a **security** hole — `DOCUMENT_URL_SIGNING_SECRET` shipped a constant default with no prod guard, making download tokens forgeable (any `documents.view` user could fetch any clean doc incl. signature images); (2) a **migration-abort** — the F1 back-fill's set-based `NOT EXISTS` guard would violate `students.person_id` unique when ≥2 students share one account. Both fixed + proven (prod-required secret like ENCRYPTION*KEY; `DISTINCT ON` back-fill, verified against a crafted collision). Also fixed 3 moderates (rollback no longer deletes pre-existing Persons — `import_rows.target_created`; `approve` requires validation; signature docs forced restricted) + a size-cap nit. e2e 15/15 on fresh pg; check:privileged-db + check-types green. Non-blocking follow-ups **F2-fu1/2/3** added above. *(claude)\_
- `2026-08-01` — **F1 + F4 + F2 built to DoD → in-review.** Three Phase-1 foundations shipped on `feat/phase1-foundations-f1-f2-f4` → **[PR #42](https://github.com/Ewosoft-Solutions/claude-trial/pull/42)** (open, awaiting review; GitHub Actions runs CI on the PR). **F1** (ADR-01): `person` schema — Person/ContactPoint/Address/StaffProfile/GuardianRelationship/RelationshipHistory + additive `students.person_id`, RLS-safe DO-block back-fill, PersonService + PersonMergeService (dedup with preserved history), contact masking. **F4** (ADR-08): `documents` schema — Document/DocumentVersion/DocumentType/SigningAuthority/SignatureUse on the StorageProvider port, HMAC signed short-lived downloads, scan+thumbnail on F3 jobs (quarantine-until-clean), governed signature use. **F2** (ADR-09): `imports` schema — full 11-entity set, idempotent upsert-by-source-ref commit into Person (F1), source-file storage+scan via Document (F4), explicit exception queue, count/sum/checksum reconciliation, controlled rollback, step-up on commit. Permissions +15 (305→320). **Validation (real pg16-parity throwaway):** e2e F1 4/4 · F4 6/6 · F2 4/4 · F3 7/7 (serialized suite 21/21); api unit 444/444; ui 120/120; web 115/115; `db:rls:check` green (person/documents/imports covered); seed 320/320; `pnpm ci:quick` green. e2e suite set to `--runInBand` (shared-DB global job worker). **Next:** [PR #42](https://github.com/Ewosoft-Solutions/claude-trial/pull/42) open → second-agent review (L/XL) → merge; then WB1 (People directory) unblocked by F1. _(Separately, [PR #43](https://github.com/Ewosoft-Solutions/claude-trial/pull/43) adds a changed-files Prettier gate + editor format-on-save — infra, not a board item.)_ _(claude)_
- `2026-08-01` — **F4 + F1 + F2 claimed (claude).** Building Phase-1 foundations F4 (document/attachment service, ADR-08), F1 (Person/identity/profile/membership, ADR-01), and F2 (import & migration platform, ADR-09) to DoD on a single branch `feat/phase1-foundations-f1-f2-f4`. Build order F4 → F1 → F2 (F2 reuses Person + Document). Owner steers: foundation-to-DoD (models + RLS migration + server-side services/enforcement + tests + minimal REST; workbench UI deferred to WB items), full F2 entity set. _(claude)_
- `2026-08-01` — **F3 done → F2/F4/F5 unblocked.** Durable job queue + transactional outbox + idempotency shipped (ADR-06): new `jobs` schema (`jobs.jobs`, `jobs.outbox_events`) with RLS + `app_runtime` grants + `db:rls:check` coverage; `JobService.enqueue` (atomic-with-domain, idempotent via ON CONFLICT), `OutboxService.emit`, and a `JobWorker` that claims under the audited `app.is_platform` scope and runs handlers under per-job tenant scope with handler+completion in one tx (exactly-once), retry/backoff→`dead`, stale-lock reclaim. Coexists with the legacy in-memory `QueueService`. e2e 7/7 (idempotency, exactly-once, retry→dead, tx-atomic, RLS isolation); check:privileged-db + db:rls:check + ci:quick green. _(claude)_
- `2026-08-01` — **P0-3: 7 non-owner ADRs accepted → `F1` unblocked.** Reviewed + accepted ADR-01/02/06/07/08/09/12 (`Accepted — 2026-08-01`). Flips **F1 (Person) → ready** and confirms the design for the already-ready `F3`/`F7`/`F8`; F2/F5 now blocked only on F3. Reconciled a doc drift: **ADR-04 is owner-gated** (Q13–16 result-publication policy — the index had mislabelled it "no"), so the batch is **7/12**; **5 stay owner-gated** — ADR-03 (Q11), ADR-04 (Q13–16), ADR-05 (Q20–23), ADR-10 (Q20), ADR-11 (Q6) — keeping F6 + WB2/4/5 blocked. Phase-1 foundations `F1`/`F3`/`F7`/`F8` are now buildable. _(claude)_
- `2026-08-01` — **Build-first sequencing (owner).** Legacy-system screenshots are our sample of what schools use → we build from those learnings + seed data to a fully-functional demo, sell on it, then tweak per school. **P0-2 resequenced** from a pre-build gate to **onboarding** (per-school redacted exports reconcile that school before its live cutover — the Q3 rule, applied per-school at go-live). Phase-0 exit gate + [`release-1-promise.md`](release-1-promise.md) (new _Sequencing_ section) updated. Build path is unblocked; nothing waits on partner recruitment. _(claude)_
- `2026-07-31` — **P0-1 decided → done.** Owner approved the recommended defaults for decision Q1–Q3: **NG K-12** is the first full profile (tertiary/TVET/international = separate scope); "retire the legacy system" = **capability parity on the critical jobs a partner actually uses**, published as a supported/excluded matrix, not menu parity; **operational evidence + reconciliation outrank screenshots** for high-consequence behaviour. Deliverable: [`release-1-promise.md`](release-1-promise.md). Unblocks profile-specific scope in every workbench + P0-2's export requirement. **Still open:** ADR-10 (Q19–20) + ADR-11 (Q6) remain owner-gated; the definitive per-partner matrix awaits **P0-2**. _(claude)_
- `2026-07-31` — **H3 approved → done.** Audited authenticated workspaces: no commercial subscription nag ships today. Added a regression guard that rejects subscription/trial/upgrade/renewal/premium nags and the legacy expiry-linked “Pay Now” signature while preserving school-fee and finance actions. PR [#36](https://github.com/Ewosoft-Solutions/claude-trial/pull/36). _(codex)_
- `2026-07-31` — **H2 implemented; draft PR #35 open.** Added explicit Aurora permission-denied and full-surface offline presets, state-system tests, design-system examples, and shared-state wiring for `/unauthorized`. Awaiting review/merge. _(codex)_
- `2026-07-31` — **H1 implemented; draft PR #34 open.** Current status docs now reflect the wired web/API system and active product-expansion initiative; operational permission references and verification use the enforced 305-permission catalog (9 persisted categories, 28 seed groups). Root validation aliases were restored. Awaiting review/merge. _(codex)_
- `2026-07-31` — **ADR batch 3 drafted — ALL 12 ADRs now drafted.** ADR-05 (finance subledger), ADR-12 (interop) as full ADRs; ADR-10 (GL build-vs-integrate) + ADR-11 (tenant/campus) as **owner decision-briefs**. Next: review→accept (`P0-3`); owner sign-off outstanding on ADR-03/04/05/10/11. _(claude)_
- `2026-07-31` — **ADR batch 2 drafted** (ADR-02 class/offering, ADR-03 curriculum, ADR-04 result publication, ADR-09 migration) — `Proposed`. **9 of 12 ADRs now drafted**; only ADR-05 (finance), ADR-10 (GL build-vs-integrate, owner), ADR-11 (tenant/campus, owner), ADR-12 (interop) remain. _(claude)_
- `2026-07-31` — **ADR batch 1 drafted** (ADR-01 person, ADR-06 job/outbox, ADR-07 delivery, ADR-08 document/signature) — `Proposed`, awaiting review/acceptance. Accepting them flips `F1`, `F4`, `F5` (and their dependents) `blocked → ready`. _(claude)_
- `2026-07-31` — Board created from parity matrix + roadmap. All near-term items seeded; Phase-0 ADRs opened; owner-gated items marked blocked. _(claude)_
